# F3.5b — Code Review + UI Review

> Etapa [10] do workflow. Data: 2026-05-17. Escopo: F3.5a (redesign de charts)
> + F3.5b (seletor de período). 32 arquivos, +1394/−264.
> Verificação: `tsc` limpo, `eslint` limpo, `jest` 237/237, `next build` ok.

## Code review

Auditoria de bugs, segurança e qualidade nos arquivos da fase.

**Críticos:** nenhum.
**Importantes:** nenhum.

**Observações (Info) — sem ação obrigatória:**

- `periodo.ts` — lógica pura coberta por 19 casos de teste (presets, custom
  parcial/inválido, `de>ate`, recorte de mês futuro, ida-e-volta
  `periodoParaParams`/`resolverPeriodo`). Aritmética de mês via índice absoluto
  `ano*12+mês`, sem dependência de fuso além de UTC.
- `page.tsx` — período resolvido uma vez e injetado em `periodoDe`/`periodoAte`;
  preset `tudo` zera o recorte, comportamento já suportado pelas queries.
- `custom-range-popover.tsx` — meses futuros desabilitados via `disabled` +
  `aria-disabled`; "Aplicar" gated por `de && ate`. Sem caminho que dispare
  `onAplicar` com intervalo inválido.
- `period-bar.tsx` — `aplicar()` deleta `periodo`/`de`/`ate` antes de
  reescrever, garantindo que trocar de `custom` para preset não vaza `de`/`ate`
  na URL (coberto por teste).
- Segurança: nenhum dado sensível, nenhuma query nova; o RBAC por domínio
  (`requireDomainAccess`) em `page.tsx` permanece intacto. Estado só na URL.
- Teste do `CustomRangePopover` mocka o wrapper `ui/popover` porque o
  `Positioner` do base-ui depende de layout real e quebra no jsdom — o mock é
  fiel ao contrato `open`/`onOpenChange`; o posicionamento real é
  responsabilidade de terceiros e não regride a lógica do componente.

## UI review (6 pilares — `ui-ux-pro-max`)

1. **Acessibilidade** — `PeriodBar` é `role="radiogroup"` com `radio` +
   `aria-checked`; chevrons e meses têm `aria-label`; foco visível
   (`focus-visible:ring`) em todos os controles. ✓
2. **Toque/interação** — meses da grade `h-11` (44px) ✓. Pílulas de período
   `h-9` (36px) e chevrons `size-8` (32px) ficam abaixo de 44px — **FLAG
   menor**: aceitável num dashboard desktop-first e coerente com a referência
   compacta do `nexus-insights`; revisitar se houver uso mobile intenso.
3. **Consistência** — tokens semânticos (`bg-primary`, `bg-muted`,
   `text-muted-foreground`), ícones lucide, mesmo vocabulário visual do
   `ChartCard`/design system. ✓
4. **Estados** — hover, `focus-visible`, ativo e desabilitado (mês futuro)
   visualmente distintos. ✓
5. **Responsivo** — fileira de pílulas com `flex-wrap`; popover com largura
   fixa `w-72`. ✓
6. **Feedback/animação** — transições 150–200ms; popover com `data-open`/
   `data-closed` (fade/zoom). `prefers-reduced-motion` respeitado pelos charts
   (F3.5a). ✓

**Veredito:** sem bloqueador. Um FLAG menor de alvo de toque, registrado e
aceito para o contexto desktop. Fase liberada.
