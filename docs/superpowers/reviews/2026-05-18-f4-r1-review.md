# Review — Fix R1 (re-source de `fato_financeiro_titulo`)

**Data:** 2026-05-18 · **Branch:** `feat/f4-financeiro-titulos` · commits `87309ef`, `9c984fa`.
**Revisor:** Claude (review de código + verificação end-to-end contra o cache real).

## Veredito: APROVADO — 0 críticos, 0 importantes, 0 menores.

## O que foi revisado
- **Builder `fato-financeiro-titulo.ts`:** fonte trocada para `rawFinanLancamento`,
  filtro `rawDeleted=false` + `tipo IN ('a_receber','a_pagar')` (descarta os
  lançamentos de caixa `recebimento`/`pagamento`/`entrada`/`saida`). `tipo` agora
  vem direto do campo `selection` da fonte — sem a derivação frágil por `sinal`
  da versão anterior. Mapeamento de datas com fuso local mantido. Padrão de
  rebuild (`deleteMany`+`createMany`+`markFatoBuilt(tx)`) intacto.
- **Queries `financeiro.ts`:** critério `situacaoSimples='aberto'` mantido; valor
  e totais (`totalAReceber/Pagar/Vencido`) voltam a usar `vrSaldo` — que na fonte
  correta (`finan.lancamento`) é o valor real do título em aberto.
- **Tools MCP + testes:** `vrSaldo` re-incluído no `outputSchema`/shaping;
  fixtures dos testes alinhados ao formato real de `finan.lancamento`.

## Verificação end-to-end (contra o cache real)
- `rebuildFatoFinanceiroTitulo` → **168 linhas** (139 a_receber + 29 a_pagar,
  filtrados de 208 lançamentos).
- Servidor MCP, tools chamadas como cliente real (super_admin):
  - `financeiro_contas_a_receber` → **120 títulos / R$ 1.164.266,36**
  - `financeiro_contas_a_pagar` → **18 títulos / R$ 95.694,95**
  - `financeiro_titulos_vencidos` → **13 títulos / R$ 189.730,78**
- Bate exatamente com a consulta SQL direta ao `raw_finan_lancamento`.
- `tsc` (raiz + mcp), `jest` 646/646 — verdes.

## Observações
- `raw_finan_pagamento_divida` ficou órfã (nenhum fato a consome). Permanece
  sincronizada no cache — sem resíduo de código (nenhum import morto).
- `docs/RADAR.md` R1 marcado RESOLVIDO; doc de fontes de financeiro atualizado.
