# Fase 2.5 , Unificação + Confiabilidade do Faturamento , Implementation Plan (v3)

> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:test-driven-development` por task.
> Execução INLINE na sessão principal (Opus), conforme CLAUDE.md §6[8]. Steps com checkbox.
> SPEC v3: `docs/superpowers/specs/2026-06-10-f2.5-unificacao-faturamento-design.md`.

**Goal:** Unificar o faturamento na camada canônica (receita externa real como headline do
grupo; individual honesto por empresa), blindar a marcação intercompany (whitelist) e
padronizar período, tudo travado por conferência (TS×SQL) e E2E real.

**Architecture:** Core `carregarItensVendaComGrupo` (groupBy itens + findMany notas, marcação
por nota, `Map` para data) → `receitaConsolidada` refatorada (saída idêntica) + 2 métricas novas.
Cada tool de faturamento entrega handler + formatador + teste **no mesmo commit** (sem janela
de runtime quebrado). Whitelist de participante_id como 1ª camada de `ehNotaIntragrupo`.
Conferência ganha `checkBanda`/`checkGte` + S0..S4. Baseline pré-whitelist gerado com whitelist
vazia (prova mecânica de "não reduz").

**Tech Stack:** TypeScript, Prisma v7 (`@/generated/prisma/client`), Zod, Jest (ts-jest),
tsx (conferência/E2E). MCP `@modelcontextprotocol/sdk`.

> **§0 , Achados das 2 reviews adversariais do plano (aplicados nesta v3):**
> - Tasks de teste são **co-locadas** (`<x>.test.ts` ao lado do fonte), NÃO em `__tests__/`
>   para `metrics/fiscal` (mas `src/lib/fiscal/grupo/__tests__/` existe , Tasks 1/2 usam-no).
> - **Onda 4+5 fundidas por tool** (handler+formatador+teste/commit) , evita resposta errada
>   em runtime entre commits (tsc não pega).
> - **Task 15 decomposta** em 1 task/tool (Grupo B) + Grupo C seletivo.
> - **Task 0** gera baseline-eliminação **antes** da whitelist (via whitelist vazia), senão S0
>   é tautológico.
> - **Whitelist** = curadoria validada (recicladas casam por nome; pid 24 a decidir); S0 é a
>   garantia mecânica de que não reduz.
> - Core sem `O(n·m)`: `Map` de data embutido. `data_emissao` é meia-noite UTC (0 notas com
>   hora≠0) → `getUTCMonth` sem off-by-one.
> - **E2E `f2-receita-consolidada.e2e.ts` roda logo após o refactor** (Task 5), não só no fim.
> - Snapshot de catálogo **não** regenera (não captura `outputSchema`); inputSchema preservado.
> - kpiOuro cov-68 cravado com `valor/match:faixa/delta/fonteOuro` + `volatil:true`.

---

## Mapa de arquivos

**Criar (isoláveis):**
- `src/lib/fiscal/grupo/whitelist-grupo.ts` + `__tests__/whitelist-grupo.test.ts` (Task 1)
- `src/lib/metrics/fiscal/_itens-venda-grupo.ts` + `_itens-venda-grupo.test.ts` (Task 4)
- `src/lib/metrics/fiscal/faturamento-serie-mensal.ts` + `.test.ts` (Task 6)
- `src/lib/metrics/fiscal/faturamento-por-cliente-canon.ts` + `.test.ts` (Task 7)
- `docs/superpowers/research/baseline-eliminacao-pre-whitelist.json` (Task 0, gerado)
- `scripts/gen-baseline-eliminacao.ts` (Task 0, one-shot)

**Modificar (quentes , integração inline):**
- `src/lib/fiscal/grupo/participantes-grupo.ts`, `index.ts` (Tasks 2/3)
- `src/lib/metrics/fiscal/receita-consolidada.ts` (+ `receita-consolidada.test.ts`), `index.ts` (Tasks 5/8)
- `mcp/tools/fiscal/faturamento-periodo.ts` + `mcp/lib/responder.ts` (Task 9a)
- `mcp/tools/fiscal/faturamento-por-cliente.ts` + responder (Task 10a)
- `mcp/tools/fiscal/faturamento-mensal-serie.ts` + responder (Task 11a)
- 7 tools Grupo B (Tasks 15a-15g) + Grupo C (15h/15i)
- `scripts/conferencia-fiscal.ts` (Tasks 0/16/17a-c)
- `src/lib/agent/evals/golden/golden-nex.json` (Task 21), `docs/RADAR.md`, PROGRESSO (Task 22)

---

## ONDA 0 , Baseline pré-whitelist (gate S0 não-tautológico)

### Task 0: gerar `baseline-eliminacao-pre-whitelist.json`

**Files:** Create `scripts/gen-baseline-eliminacao.ts`, `docs/superpowers/research/baseline-eliminacao-pre-whitelist.json`.

Objetivo: capturar `receitaIntragrupoEliminavel` por período **com a marcação ATUAL** (whitelist
ainda não existe / equivale a whitelist vazia). É o número que a whitelist não pode reduzir.

- [ ] **Step 1:** escrever o one-shot. Como a whitelist ainda não está no caminho (Task 2 só vem
  depois), basta chamar `receitaConsolidada` atual nos 5 períodos e gravar `receitaIntragrupoEliminavel`.

```ts
// scripts/gen-baseline-eliminacao.ts
import { prisma } from "@/lib/prisma";
import { receitaConsolidada } from "@/lib/metrics/fiscal/receita-consolidada";
import { writeFileSync } from "node:fs";
const periodos = [
  { rotulo: "2023", de: "2023-01-01", ate: "2023-12-31" },
  { rotulo: "2024", de: "2024-01-01", ate: "2024-12-31" },
  { rotulo: "2025", de: "2025-01-01", ate: "2025-12-31" },
  { rotulo: "2026 (ate jun)", de: "2026-01-01", ate: "2026-06-30" },
  { rotulo: "ACUMULADO (13 anos)", de: undefined, ate: undefined },
];
(async () => {
  if (process.env.E2E !== "1") { console.log("SKIP: E2E=1"); return; }
  const out: Record<string, number> = {};
  for (const p of periodos) {
    const rc = await receitaConsolidada(prisma, { periodoDe: p.de, periodoAte: p.ate });
    out[p.rotulo] = rc.receitaIntragrupoEliminavel;
    console.log(p.rotulo, rc.receitaIntragrupoEliminavel.toFixed(2));
  }
  writeFileSync("docs/superpowers/research/baseline-eliminacao-pre-whitelist.json", JSON.stringify(out, null, 2) + "\n");
  await prisma.$disconnect();
})().catch((e) => { console.error(e); process.exit(1); });
```

- [ ] **Step 2:** Run `E2E=1 npx tsx --env-file=.env.local scripts/gen-baseline-eliminacao.ts`.
  Expected: imprime 5 valores; grava o JSON. Anotar os números (esp. 2025 ≈ R$ 217,9 mi).
- [ ] **Step 3: Commit** `git add scripts/gen-baseline-eliminacao.ts docs/superpowers/research/baseline-eliminacao-pre-whitelist.json && git commit -m "chore(fiscal): baseline de eliminacao intragrupo pre-whitelist (gate S0)"`

---

## ONDA 1 , Camada de grupo (whitelist + ehNotaIntragrupo)

### Task 1: Whitelist de participante_id do grupo (curadoria VALIDADA)

**Files:** Create `src/lib/fiscal/grupo/whitelist-grupo.ts`, `src/lib/fiscal/grupo/__tests__/whitelist-grupo.test.ts`.

- [ ] **Step 1: Derivar e classificar candidatos no cache real** (NÃO copiar lista cega):

Run o script de derivação que, para cada participante_id que aparece em nota de saída autorizada
cujo nome casa `RAIZES_GRUPO`, imprime: id, nome (na nota), documento_digits (fato_parceiro),
nº de notas 2025, nº de notas total.

```ts
// inspecao ad-hoc (-e): para cada candidato, imprimir id|nomeNota|docCadastro|nNotas2025|nNotasTotal
```

Classificar:
- **GRUPO (whitelist):** nome de estabelecimento do grupo E (documento do cadastro com raiz do
  grupo OU documento vazio MAS com notas recorrentes recentes). Esperado: `{2,9,10,11,12,13,14,15,16,19,20,21,22,23}`.
- **DECIDIR pid 24:** a review do plano achou pid 24 casando nome + presente no cadastro , se
  confirmar (documento do cadastro com raiz do grupo OU notas recorrentes), **incluir** (vira 15 ids).
  Documentar a decisão no comentário do arquivo.
- **RECICLADO (excluir):** nome de terceiro conhecido (Vilmar/Smartfit/Jaguaribe/Residencial) OU
  documento do cadastro com raiz FORA do grupo. Esperado: `{8722,8723,9552,7719}`.

- [ ] **Step 2: Escrever o teste** (com a lista final decidida no Step 1 , exemplo com 14, ajustar se 24 entrar):

```ts
import { PARTICIPANTES_GRUPO_WHITELIST, PARTICIPANTES_RECICLADOS_EXCLUIDOS } from "../whitelist-grupo";
describe("PARTICIPANTES_GRUPO_WHITELIST", () => {
  it("contem os participante_id do grupo validados no cache", () => {
    expect([...PARTICIPANTES_GRUPO_WHITELIST].sort((a, b) => a - b))
      .toEqual([2, 9, 10, 11, 12, 13, 14, 15, 16, 19, 20, 21, 22, 23]); // +24 se decidido
  });
  it("NAO inclui odoo_id reciclados", () => {
    for (const id of [8722, 8723, 9552, 7719]) {
      expect(PARTICIPANTES_GRUPO_WHITELIST.has(id)).toBe(false);
      expect(PARTICIPANTES_RECICLADOS_EXCLUIDOS.has(id)).toBe(true);
    }
  });
  it("NAO inclui franquias Matrix Fit (clientes externos)", () => {
    for (const id of [7672, 16003]) expect(PARTICIPANTES_GRUPO_WHITELIST.has(id)).toBe(false);
  });
});
```

- [ ] **Step 3: Rodar e ver falhar** → FAIL (módulo inexistente).
- [ ] **Step 4: Implementar** `whitelist-grupo.ts` com a lista validada + comentário do critério e da
  decisão do pid 24, e `PARTICIPANTES_RECICLADOS_EXCLUIDOS = {8722,8723,9552,7719}` com a razão
  (odoo_id reciclados: Vilmar/Smartfit/Jaguaribe/Residencial).
- [ ] **Step 5: Rodar e ver passar** → PASS.
- [ ] **Step 6: Commit** `git commit -m "feat(fiscal): whitelist participante_id do grupo (validada no cache)"`

### Task 2: `ehNotaIntragrupo` evoluído (whitelist → cadastro → nome)
(igual ao plano v1 Task 2: adicionar import da whitelist como 1ª camada; 3 testes novos; manter os antigos verdes; commit). **Após esta task, S0 protege contra qualquer redução.**

### Task 3: Exportar whitelist no barrel `src/lib/fiscal/grupo/index.ts` + `tsc` + commit.

---

## ONDA 2 , Core compartilhado + refactor receitaConsolidada

### Task 4: Core `carregarItensVendaComGrupo` (com `Map` de data, sem O(n·m))

**Files:** Create `src/lib/metrics/fiscal/_itens-venda-grupo.ts`, `src/lib/metrics/fiscal/_itens-venda-grupo.test.ts` (co-locado).

- [ ] **Step 1: Teste** (mock de prisma; fixtures: nota 100 externa venda R$2000 mês 3, nota 200
  intragrupo venda R$1000 mês 4, nota 300 transferência R$500 mês 4). Asserts: itens com `intragrupo`/
  `ehReceita`/`mesEmissao` corretos; `marcacaoPorNota.get(200).intragrupo === true`. (mock inclui
  `dataEmissao` nas notas , o core lê data via Map.)
- [ ] **Step 2: Falhar.**
- [ ] **Step 3: Implementar** (extrai o padrão de `receita-consolidada.ts`; `dataPorNota` Map embutido):

```ts
// ... imports iguais ao plano v1 ...
export interface ItemVendaGrupo { documentoId: number|null; cfopId: number|null; valorProdutos: number; ehReceita: boolean; intragrupo: boolean; participanteId: number|null; participanteNome: string|null; empresaId: number|null; empresaNome: string|null; mesEmissao: number|null; }
export interface MarcacaoNota { intragrupo: boolean; participanteId: number|null; participanteNome: string|null; empresaId: number|null; empresaNome: string|null; }
export interface ItensVendaGrupoResultado { itens: ItemVendaGrupo[]; marcacaoPorNota: Map<number, MarcacaoNota>; participantesGrupo: Set<number>; }

export async function carregarItensVendaComGrupo(prisma, input): Promise<ItensVendaGrupoResultado> {
  const whereCommon = { entradaSaida: "1" as const, situacaoNfe: "autorizada" as const, ...buildPeriodoWhere(input.periodoDe, input.periodoAte), ...buildEmpresaWhere(input.empresaId) };
  const grupos = await prisma.fatoNotaFiscalItem.groupBy({ by: ["documentoId", "cfopId"], _sum: { vrProdutos: true }, _count: true, where: whereCommon as Prisma.FatoNotaFiscalItemWhereInput });
  const ids = [...new Set(grupos.map((g) => g.cfopId).filter((x): x is number => x !== null))];
  const nomeRows = ids.length ? await prisma.fatoNotaFiscalItem.findMany({ where: { cfopId: { in: ids } }, select: { cfopId: true, cfopNome: true }, distinct: ["cfopId"] }) : [];
  const ehReceitaPorCfop = new Map<number, boolean>();
  for (const r of nomeRows) { if (r.cfopId !== null) ehReceitaPorCfop.set(r.cfopId, classificarCfop(extrairCfop(r.cfopNome)).ehReceita); }
  const notas = await prisma.fatoNotaFiscal.findMany({ where: whereCommon as Prisma.FatoNotaFiscalWhereInput, select: { odooId: true, participanteId: true, participanteNome: true, empresaId: true, empresaNome: true, dataEmissao: true } });
  const participantesGrupo = await carregarParticipantesGrupo(prisma);
  const marcacaoPorNota = new Map<number, MarcacaoNota>();
  const dataPorNota = new Map<number, Date | null>();   // sem O(n*m)
  for (const n of notas) {
    marcacaoPorNota.set(n.odooId, { intragrupo: ehNotaIntragrupo(n, participantesGrupo), participanteId: n.participanteId, participanteNome: n.participanteNome, empresaId: n.empresaId, empresaNome: n.empresaNome });
    dataPorNota.set(n.odooId, n.dataEmissao ?? null);
  }
  const itens: ItemVendaGrupo[] = grupos.map((g) => {
    const m = g.documentoId !== null ? marcacaoPorNota.get(g.documentoId) : undefined;
    const data = g.documentoId !== null ? dataPorNota.get(g.documentoId) ?? null : null;
    return { documentoId: g.documentoId, cfopId: g.cfopId, valorProdutos: Number(g._sum.vrProdutos ?? 0), ehReceita: g.cfopId !== null ? (ehReceitaPorCfop.get(g.cfopId) ?? false) : false, intragrupo: m?.intragrupo ?? false, participanteId: m?.participanteId ?? null, participanteNome: m?.participanteNome ?? null, empresaId: m?.empresaId ?? null, empresaNome: m?.empresaNome ?? null, mesEmissao: data ? data.getUTCMonth() + 1 : null };
  });
  return { itens, marcacaoPorNota, participantesGrupo };
}
```

- [ ] **Step 4: Passar.** **Step 5: Commit.**

### Task 5: Refatorar `receitaConsolidada` para consumir o core (saída idêntica) + provar no real

**Files:** Modify `src/lib/metrics/fiscal/receita-consolidada.ts`, ajustar `receita-consolidada.test.ts` (co-locado) se a sequência de queries exigir.

- [ ] **Step 1:** rodar `npx jest src/lib/metrics/fiscal/receita-consolidada.test.ts` → PASS (baseline).
- [ ] **Step 2:** reescrever o corpo usando o core (loop sobre `itens` + contagem via `marcacaoPorNota`,
  igual ao plano v1 Task 5). Manter interface e valores.
- [ ] **Step 3:** rodar o unit test → PASS **sem mudar valores** (ajustar o mock só se a ordem de
  queries mudar; o mock atual provavelmente sobrevive , `dataEmissao` ausente vira `null`, inócuo).
- [ ] **Step 4: PROVA NO DADO REAL (regra de raiz §6[9]):**
  - `E2E=1 npx tsx --env-file=.env.local scripts/conferencia-fiscal.ts` → I1..I5 fecham nos 5 períodos.
  - `E2E=1 npx tsx --env-file=.env.local src/lib/reports/__tests__/e2e/f2-receita-consolidada.e2e.ts`
    → bandas acumuladas (receitaExterna ~897mi, intragrupo ~418mi, intercompanyBruto ~700mi,
    `notasIntragrupo ∈ [6000,6600]`) **inalteradas**. Se quebrar, o core errou a contagem , corrigir ANTES de commitar.
- [ ] **Step 5: tsc + Commit** `git commit -m "refactor(fiscal): receitaConsolidada consome o core (saida identica, E2E real verde)"`

---

## ONDA 3 , Métricas novas

### Task 6: `faturamentoSerieMensal` (co-locado)
(igual ao plano v1 Task 6, com a definição de `notasExternas` por mês decidida: contar nota externa
distinta com pelo menos um item no mês; `totalNotasExternasAno` reconcilia com a soma dos meses.)
Padronizar nome do parâmetro `mesLimite`. Teste + commit.

### Task 7: `faturamentoPorClienteCanon` (co-locado)
(igual ao plano v1 Task 7.) Teste + commit.

### Task 8: barrel `src/lib/metrics/fiscal/index.ts` +2 exports + tsc + commit.

---

## ONDA 4 , Tools de faturamento (handler + formatador + teste no MESMO commit)

> Cada task entrega o handler E o formatador E o teste do formatador num único commit , sem janela
> de runtime quebrado (review arq B1). `_DESTAQUE` é `Record<string, string|number>` (boolean vira 0/1).

### Task 9a: `fiscal_faturamento_periodo` , receitaConsolidada + headline + fmt

**Files:** Modify `mcp/tools/fiscal/faturamento-periodo.ts`, `mcp/lib/responder.ts`, test `mcp/lib/responder.test.ts`.

- [ ] **Step 1:** redesenhar `dados = z.object({...})` da tool **campo a campo** (review arq A-2):

```ts
const dados = z.object({
  receitaExterna: z.number(), receitaIndividual: z.number(), intragrupoEliminavel: z.number(),
  percentualEliminado: z.number(), notasExternas: z.number().int(), notasIntragrupo: z.number().int(),
  headlineValor: z.number(), headlineRotulo: z.string(), concentrador: z.boolean(), periodoLabel: z.string(),
  escopoEmpresa: z.record(z.string(), z.unknown()), aviso: z.string(),
  _RESPOSTA: z.string().optional(), _DESTAQUE: z.record(z.string(), z.union([z.string(), z.number()])).optional(),
  _agregado: z.record(z.string(), z.number().optional()).optional(),
});
```

- [ ] **Step 2:** handler (import `receitaConsolidada` de `@/lib/metrics/fiscal/index.js` + `resolverPeriodoFiscal`):
  headline por `escopo.empresaId === undefined` (grupo→externa / empresa→individual); `_DESTAQUE` com
  `headlineValor/headlineRotulo/receitaExterna/receitaIndividual/intragrupoEliminavel/percentualEliminado/notasExternas/concentrador(0|1)/periodoLabel`. (código do plano v1 Task 9 Step 3.) Preservar `inputSchema`/`id`/`descricao`.
- [ ] **Step 3:** reescrever `fmtFaturamentoPeriodo` (código do plano v1 Task 12 Step 3) + 2 testes de
  unidade (caminho grupo: "Receita externa real"; caminho concentrador: contém "concentrador").
- [ ] **Step 4:** `npx jest mcp/lib/responder.test.ts` → PASS; `npx tsc --noEmit` → 0.
- [ ] **Step 5: Commit** `git commit -m "feat(fiscal): faturamento_periodo = receita externa real (headline) + fmt"`

### Task 10a: `fiscal_faturamento_por_cliente` , canon + fmt (handler+fmt+teste/commit)

**Files:** Modify `mcp/tools/fiscal/faturamento-por-cliente.ts`, responder + test.

- [ ] **Step 1:** `dados` schema: `linhas` (participanteId/participanteNome/quantidade/valorTotal),
  `total`, `totalExterno`, `totalIntragrupo`, `topClienteExterno`, escopo, aviso, `_DESTAQUE`, `_agregado`.
- [ ] **Step 2:** handler: `resolverPeriodoFiscal` + `faturamentoPorClienteCanon`. `_DESTAQUE` =
  `{ topCliente: topClienteExterno ?? "", valorTopCliente: linhas[0]?.valorTotal ?? 0, totalExterno,
  totalIntragrupo, totalClientes: total, linhasExibidas: linhas.length, periodoLabel }`. Paginação como hoje.
- [ ] **Step 3:** `fmtFaturamentoPorCliente`:

```ts
const fmtFaturamentoPorCliente: FormatadorCanonico = (env) => {
  const d = env._DESTAQUE ?? {};
  const top = d.topCliente ? String(d.topCliente) : "";
  const valorTop = Number(d.valorTopCliente ?? 0);
  const ext = Number(d.totalExterno ?? 0);
  const intra = Number(d.totalIntragrupo ?? 0);
  const periodo = String(d.periodoLabel ?? "");
  if (!top) return `Nenhum cliente externo com faturamento no período (${periodo}).`;
  const intraStr = intra > 0 ? ` Vendas intragrupo (${formatBRL(intra)}) ficam fora deste ranking.` : "";
  return `Top cliente externo: ${top} com ${formatBRL(valorTop)}. Total externo no período (${periodo}): ${formatBRL(ext)}.${intraStr}`;
};
```
  + teste de unidade.
- [ ] **Step 4: tsc + jest.** **Step 5: Commit.**

### Task 11a: `fiscal_faturamento_mensal_serie` , série canônica + fmt (handler+fmt+teste/commit)

**Files:** Modify `mcp/tools/fiscal/faturamento-mensal-serie.ts`, responder + test.

- [ ] **Step 1:** `dados.serie` agora `{mes, individual, externa, intragrupoEliminavel, notasExternas}`;
  agregados `{ totalExternaAno, totalIndividualAno, totalNotasExternasAno }`. Preservar input `ano`.
- [ ] **Step 2:** handler: `const mesLimite = ano === hoje.getFullYear() ? hoje.getUTCMonth()+1 : 12;`
  `faturamentoSerieMensal(ctx.prisma, { ano, empresaId: escopo.empresaId, mesLimite })`. `_DESTAQUE` =
  `{ ano, totalExternaAno, totalIndividualAno, totalNotasExternasAno, mesesConsultados: serie.length }`.
- [ ] **Step 3:** `fmtFaturamentoMensalSerie`: média mensal sobre `externa`; cabeçalho "Receita externa
  de {ano}: {totalExternaAno} em {meses} meses (individual {totalIndividualAno})." + teste.
- [ ] **Step 4: tsc + jest.** **Step 5: Commit.**

---

## ONDA 5 , Fix de período (Grupo B: 1 task/tool; Grupo C: seletivo)

> Padrão de cada Grupo B (idêntico): importar `resolverPeriodoFiscal`; no handler
> `const per = resolverPeriodoFiscal(input.periodoDe, input.periodoAte)`; passar `per.periodoDe/Ate`
> à query; `aviso` recebe `Período: ${per.label}.`. NÃO mudar inputSchema/id/descricao. tsc + commit.
> **NÃO mexer em `_DESTAQUE`/`_agregado`** dessas (review arq B4: só o `aviso` muda).

- [ ] **Task 15a:** `mcp/tools/fiscal/notas-emitidas.ts` (query `queryNotasEmitidas`).
- [ ] **Task 15b:** `mcp/tools/fiscal/impostos-periodo.ts` (`queryImpostosPeriodo`).
- [ ] **Task 15c:** `mcp/tools/fiscal/produtos-faturados.ts` (`queryProdutosFaturados`).
- [ ] **Task 15d:** `mcp/tools/fiscal/faturamento-nao-autorizado.ts` (`faturamentoNaoAutorizado`).
- [ ] **Task 15e:** `mcp/tools/fiscal/faturamento-por-operacao.ts` (`faturamentoPorOperacao`).
- [ ] **Task 15f:** `mcp/tools/fiscal/faturamento-por-empresa.ts` (`faturamentoPorEmpresa`).
- [ ] **Task 15g:** `mcp/tools/fiscal/faturamento-recebido.ts` (`faturamentoRecebido`).
- [ ] **Task 15h , Grupo C (semântica DIFERENTE):** `dfe-*` e `notas-recebidas` , **NÃO** usar default
  ano corrente; manter sem corte de ano (ordenação desc), aviso "todas as pendentes/recentes" + label
  do período só quando o usuário informar. Commit.
- [ ] **Task 15i , complementar:** `fiscal-complementar.ts` (apuração/mdfe/reinf) , `resolverPeriodoFiscal`
  só onde há janela; certificados sem corte. Commit.
- [ ] **NÃO TOCAR `fiscal_contar_notas`** (sem período; `ouro-fiscal-01` crava 49.427).

---

## ONDA 6 , Conferência (primitivas + S0..S4)

### Task 16: `checkBanda` + `checkGte` + S0 (eliminação não reduz)

**Files:** Modify `scripts/conferencia-fiscal.ts`.

- [ ] **Step 1:** adicionar primitivas:

```ts
function checkBanda(nome: string, valor: number, min: number, max: number, erros: string[]): void {
  const ok = valor >= min && valor <= max;
  console.log(`   ${ok ? "OK  " : "XX  "} ${nome}: ${brl(valor)} em [${brl(min)}, ${brl(max)}]`);
  if (!ok) erros.push(nome);
}
function checkGte(nome: string, valor: number, minimo: number, erros: string[]): void {
  const ok = valor >= minimo - TOL;
  console.log(`   ${ok ? "OK  " : "XX  "} ${nome}: ${brl(valor)} >= ${brl(minimo)}`);
  if (!ok) erros.push(nome);
}
```

- [ ] **Step 2:** importar o baseline JSON e, no loop, **S0 (gate):**
  `checkGte("S0 eliminacao nao reduz", rc.receitaIntragrupoEliminavel, BASELINE_ELIM[p.rotulo] ?? 0, erros);`
- [ ] **Step 3:** rodar conferência → S0 verde nos 5 períodos. Commit.

### Task 17a: S4 guarda situação=autorizada (gate)
- [ ] SQL bruto somando `vr_produtos` de itens com `situacao_nfe='em_digitacao'` (saída) e checar que
  esse valor **não** está dentro de `f1.totalProdutos` (i.e., `f1.totalProdutos` == soma só de autorizada,
  já garantido por I1; S4 torna explícito logando o valor em_digitacao como "excluído"). Gate se I1 já
  não cobrir. Commit.

### Task 17b: S1 fallback só-por-nome (alerta, banda por período)
- [ ] Definir sem ambiguidade: contar notas (saída autorizada) cuja marcação intragrupo viria
  **SOMENTE do regex de nome** se whitelist E cadastro estivessem vazios (mede fragilidade do cadastro,
  independe da whitelist). **Validar o número real por SELECT** antes de cravar a banda (esperado 2025 ≈ 783).
  Banda 2025 `[600, 1000]`; acumulado `[2200, 2900]`. Alerta (não gate). Commit.

### Task 17c: S2 divergência nome×cadastro (alerta) + S3 (gate)
- [ ] S2: contar participantes com nome de raiz do grupo mas `fato_parceiro` com doc de fora; banda `[0, 20]`. Alerta.
- [ ] S3 (gate): por ano `checkGte`/`check` que `rc.receitaExterna + rc.receitaIntragrupoEliminavel == rc.receitaIndividualTotal` (reforça I4 com whitelist no caminho). Commit.

---

## ONDA 7 , Transparência receita-perdida (item 5 , se couber, senão Fase 2.6)

### Task 18: decompor sem_cfop + 5949/6949 por `finalidade_nfe` (exibição)
- [ ] Estender o formatador/saída de `fiscal_faturamento_por_cfop` para decompor, em runtime, a linha
  `sem_cfop` e a categoria `outras` (CFOP 5949/6949) por finalidade. Teste de unidade. Commit.
  **Se o escopo apertar → mover para Fase 2.6** (documentar no RADAR).

---

## ONDA 8 , Verificação + E2E + golden + baseline + RADAR + PR

### Task 19: rebuild containers + conferência + jest completo
- [ ] **Verificar worker:** `grep -rE "ehNotaIntragrupo|carregarParticipantesGrupo|carregarItensVendaComGrupo" src/worker` , se houver, rebuildar `app` (armadilha §2.1); senão só `mcp`.
- [ ] `docker compose up -d --build mcp` (+ `docker compose build app && docker compose up -d --force-recreate worker` se o grep acima achou). Confirmar data da imagem = AGORA.
- [ ] `E2E=1 npx tsx --env-file=.env.local scripts/conferencia-fiscal.ts` → I1..I5 + S0..S4 fecham.
- [ ] `npx jest --silent` → suite COMPLETA verde: `integration.test.ts` (**104** tools), `golden-schema`,
  `golden-to-oraculo` (**frozen-30 = 30**, imune pois ids/toolEsperada preservados). Snapshot NÃO regenera.

### Task 20: E2E real + baseline diff controlado
- [ ] `f2-receita-consolidada.e2e.ts` → bandas inalteradas (já rodado na Task 5; reconfirmar).
- [ ] **Baseline f4 (review arq B4):** rodar `f4-baseline.e2e.ts`; **diffar `f4-baseline.json`**. Mapa
  esperado (documentar no PR):
  - `fiscal_faturamento_periodo`: `_DESTAQUE.{totalNotas,valorFaturado}`→`{headlineValor,...}`; `_agregado.soma` 1,37bi→~externa.
  - `fiscal_faturamento_por_cliente`: `topCliente`/`totalGeral`→cliente externo/`totalExterno`.
  - `fiscal_faturamento_mensal_serie`: `totalAno`→`totalExternaAno/totalIndividualAno`.
  - **Nenhuma outra tool** (por_cfop/por_operacao/por_empresa/por_marca/por_uf/recebido/nao_autorizado)
    pode mudar `_DESTAQUE`/`_agregado` , só ganham label de período no `aviso`. Se mudarem, é regressão.
  - Regravar com `BASELINE_WRITE=1` **só após inspeção do diff**; anexar diff ao PR.
- [ ] E2E manual no MCP/Playground: "qual o faturamento em 2025?" → receita externa ~R$ 325 mi;
  "faturamento da Jds Matriz 2025" → individual + flag concentrador.

### Task 21: kpiOuro receita externa (cov-68) , trava numérica real
- [ ] Adicionar `kpiOuro` em `cov-68` (`fiscal_receita_consolidada`) no `golden-nex.json`:
  `{ chave: "receitaExterna", match: "faixa", delta: 5_000_000, valor: <SELECT real do ano da pergunta>,
  fonteOuro: "<query>", volatil: true }`. Confirmar `golden-schema.test.ts` PASS e `ouro-fiscal-01` intacto.
  Se a pergunta cov-68 não fixa ano, usar período acumulado com faixa larga. (Se atritar com o schema,
  registrar e seguir , é oportunidade, não bloqueador.)

### Task 22: RADAR + PROGRESSO + PR + merge
- [ ] `docs/RADAR.md`: fechar R-faturamento-duas-definicoes e R-intercompany-fallback-fragil (números
  reais: +69% antes / receita externa real depois; whitelist delta 0; S0..S4); R-periodo-acumulado →
  fechado (exceto Grupo C documentado); abrir Fase 2.6 se itens 4/5 ficaram.
- [ ] PROGRESSO: Fase 2.5 concluída com números cravados.
- [ ] Commit final; PR com body detalhado (antes/depois, conferência verde, jest verde, baseline diff
  anexado). Acompanhar CI/deploy. Merge autorizado (durável), não parar.

---

## Self-Review (cobertura da spec , v3)

- §2.1/§2.2 headline+concentrador → 9a/fmt. §4.2 core → 4 (Map, sem O(n·m)). §4.3 refactor+2 métricas → 5/6/7
  (+ E2E real no 5). §4.4 tools A → 9a/10a/11a (handler+fmt/commit); B → 15a-g; C → 15h/15i; contar_notas excluído.
- §4.5 whitelist validada → 1/2/3; pid 24 decidido na execução. §4.6 S0..S4+checkBanda/checkGte → 0/16/17a-c.
- §4.7 → 18 (se couber). §4.8 contrato _DESTAQUE/headline → 9a/10a/11a. §6 → 19/20/21. §9 quentes inline.
- Sem placeholders: 9a/10a/11a com código de handler E formatador; 15 decomposta 1/tool; baseline antes da whitelist.
