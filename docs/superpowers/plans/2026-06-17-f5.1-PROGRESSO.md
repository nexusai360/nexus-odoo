# F5.1 , Webhooks de WhatsApp via n8n (entrada por slug) + UI tela cheia , PROGRESSO

> **STATUS 2026-06-18: PRONTO PARA MERGE.** Todo o F5.1 (UI tela cheia de
> webhooks, recebimento por slug, auth por token Bearer, busca/filtro, card) +
> fix de auth (login + reset forçado sem logout) concluídos e verdes (tsc 0,
> eslint 0, jest 3147). `origin/main` (#127 F5 squash) integrado na branch , merge
> limpo (a branch já era superset do F5). Próximo: PR → squash-merge → auto-deploy.

> Ponto de retomada. Branch `feat/router-ativacao-r2`. TUDO LOCAL: PR/merge só
> após aprovação do usuário. F5 (base) já está em produção (PR #127).

## Decisões confirmadas pelo usuário (2026-06-17 noite)
- **Recebimento via webhook (opção B):** o usuário define o **slug/URL** ao criar
  um webhook de "receber dados"; ao marcar **"Recebe dados do WhatsApp"**, esse
  webhook passa a alimentar o Agente Nex. Pode ter vários WhatsApps = vários webhooks.
- **NÃO usar** o "Canais → WhatsApp" (modo direto). Tudo via webhook tratado pelo n8n.
- **Número da empresa = campo `business_id`** (rótulo "Número da empresa"). Vem do
  `display_phone_number` da Meta. **Obrigatório** e **único** entre receptores de
  WhatsApp; vira **tag no card**. Cadastrado na CONFIG do webhook (não no payload).
  Não pode duplicar **slug** nem **business_id**.
- **`phone_number_id` não é necessário** (o n8n roteia a resposta).
- **Telas cheias (navegação), NÃO modal**, para criar/editar webhook (config é grande).
- **Campo descrição** no webhook.
- **Seletor de eventos (saída)** por blocos estilo a referência "Roteador Webhook
  Meta", mas listando só os eventos que a PLATAFORMA emite (bloco "Agente Nex" →
  hoje só `agent.reply`), parametrizado para crescer.
- **Ajuda na tela**: quando WhatsApp, mostrar tabela de campos + JSON copiável.

## Contrato de ENTRADA (payload que o n8n manda) , FINAL
Identidade: `wa_id` (sim), `user_id` (sim, ex.: `BR.4377207372590200`).
Mensagem: `type` (sim: text/audio/image/document/video/sticker), `text` (obrigatório
em text e audio; legenda em mídia), `message_id` (sim), `timestamp` (sim, ms),
`contact_name` (não).
Mídia: objeto `media` obrigatório quando `type` é mídia: `media.url` (sim),
`media.mime_type` (sim), `media.filename`/`media.id`/`media.sha256` (não).
- Resolução: hoje por `wa_id` (com/sem nono dígito). `user_id` recebido e guardado;
  resolver POR user_id depende de campo novo no cadastro (etapa futura).
- Mídia (image/document/...): hoje aceita e guarda os campos; tratamento pela IA
  (ler PDF/imagem) é FUTURO. Por ora mídia responde aviso amigável "ainda não tratado".
- Para a IA ler mídia no futuro, o mínimo é `media.url` + `media.mime_type`.

## Estado (o que já foi feito e commitado nesta sessão)
- [x] **Disponibilidade** (`/agente/configuracao`): ordem Desativado · Super Admin ·
  Admin · Gerente · Visualizador; descrição dinâmica/breve dentro de cada bloco
  (`channelLevelDescription`). Commit `11e6612`.
- [x] **Seletor de eventos por blocos** (componente reusável): `webhook-event-catalog.ts`
  (grupo "Agente Nex" → `agent.reply`) + `webhook-event-selector.tsx` (colapsável,
  contador X/Y, selecionar todos, cor por estado, indeterminate). Commit `11e6612`.
- [x] **Schema F5.1** (`WhatsappWebhook`): `description`, `isWhatsappReceiver`,
  `businessId` (`@@unique`). Migration ADITIVA aplicada via db execute + migrate
  resolve (drift pré-existente impede `migrate dev`); `agente schema-changed` disparado.
  Commit `11e6612`.
- [x] **Server actions de webhook** (`webhooks.ts`): create/update/list com
  `description`, `isWhatsappReceiver`, `businessId`; receptor exige número; unicidade
  de `business_id` (excl. self); +tests (29 verdes). Commit (server actions F5.1).

## FEITO depois (continuação 2026-06-17 noite)
- [x] **Contrato de entrada** (`inbound-payload.ts`): `wa_id`, `user_id`, `type`
  ampliado (text/audio/image/document/video/sticker), `media{url,mime_type,...}`
  obrigatório em mídia, text obrigatório em text/audio. Cascata feita em route,
  processor (mídia responde "ainda não tratado"), envelope. 45 testes verdes.
- [x] **Roteamento por slug**: `src/lib/whatsapp/inbound-handler.ts` (núcleo
  compartilhado) + `src/app/api/hooks/[...slug]/route.ts` (acha webhook por path,
  valida isWhatsappReceiver+HMAC do secret dele, anexa business_id). Rota fixa
  legada agora delega ao handler. Testes verdes.
- [x] **Envelope** usa `businessId` (número da empresa) no lugar de `phoneNumberId`
  (renomeado em emit-reply/build-reply-data/processor + testes). `audioMediaId`
  legado mantido (download Meta direto, não usado no n8n).

## RESOLVIDO , 6ª/7ª rodada (2026-06-18 ~02h20, várias imagens)
> tsc 0, eslint 0, jest 3147 verdes.
> - **Número da empresa: revert ao sair sem confirmar** (igual à URL). `PhoneInput`
>   ganhou prop `onBlur`; o revert ignora foco que vai pro seletor de país/botão
>   (guarda por `ref.contains(relatedTarget)`).
> - **Editar: campo URL igual ao criar** (caixa única com prefixo `.../api/hooks/`).
> - **Unicidade de slug e número em tempo real**: páginas novo/editar passam
>   `existingPaths`/`existingBusinessIds` (editar exclui o próprio); erro vermelho na
>   hora ("Já existe um webhook de entrada com esse caminho." / "...de WhatsApp com
>   esse número.") e botão de criar/salvar travado até resolver.
> - **Método POST**: tag roxa (primary) com cadeado roxo, no wizard e no editar.
> - **Card da lista redesenhado**: tag colorida por tipo (verde/azul/roxo) + ícone
>   colorido por tipo (bubble/seta-baixo/seta-cima); número do WhatsApp numa linha
>   própria ("WhatsApp: +55 ...", sem ícone redundante); "Criado em ... às HH:MM:SS".
> - **Editar: Salvar só habilita com alteração real** (dirty check) e **Cancelar**
>   virou botão `outline`; **dropdown da ajuda começa fechado** no editar.
> - **Token mascarado**: `WebhookListItem.secretHint` (`••••` + últimos 5) exibido no
>   card "Token de assinatura" do editar, para o usuário identificar o token atual.

## DECISÃO + RESOLVIDO , 5ª rodada (2026-06-18 ~01h40, 6 imagens)
> tsc 0, eslint 0, jest 3147 verdes.
> **Decisão do usuário (AskUserQuestion):** auth do webhook de ENTRADA trocada de
> HMAC-SHA256 para **token fixo via `Authorization: Bearer <token>`**.
> - **Backend:** `hmac.ts` agora expõe `verifyToken` (compara o Bearer com o secret
>   em tempo constante) no lugar de `verifySignature`; `inbound-handler.ts` lê
>   `Authorization: Bearer` e valida (401 "Token inválido"). Removidos X-Timestamp/
>   X-Signature e a janela de 5 min do INBOUND. SAÍDA (`emit-reply`/`signPayload`)
>   segue com HMAC (a plataforma calcula sozinha). Testes (`hmac.test`, rota legada)
>   atualizados.
> - **Ajuda Passo 2:** simplificado , agora só `Content-Type` + `Authorization: Bearer
>   <token>`; sem cálculo de HMAC. cURLs usam `Authorization: Bearer`.
> **Mecânica do botão de confirmar (Theme A):**
> - Ajuda (Passo 1) usa só o valor CONFIRMADO (não mostra URL inválida nem deixa copiar).
> - Botão = aplicar-no-clique: ícone "certinho" (roxo preenchido quando há alteração;
>   verde quando aplicado; X vermelho quando inválido); some quando vazio e aparece com
>   animação ao digitar; submit exige confirmar.
> - URL **reverte ao sair sem confirmar** (onBlur). Número da empresa: mesma confirmação
>   + totalmente vermelho quando inválido (corrigido o anel roxo).
> - Máscara do PhoneInput voltou com o 9: **"61 98123-4567"**.

## RESOLVIDO , 4ª rodada de feedback (2026-06-18 ~00h, 3 imagens)
> tsc 0, eslint 0, jest 96 verdes.
> - **URL desalinhada → caixa única**: prefixo (`.../api/hooks/`) + input num só box
>   (padrão do PhoneInput), borda fica TODA vermelha quando inválido (não só o input).
> - **Botão "certinho" virou botão de CONFIRMAÇÃO** (`FieldValidateButton` com
>   `variant`): roxo PREENCHIDO+seta quando há alteração válida a confirmar; clicar
>   confirma e dispara **toast** (canto inferior direito) "Endereço/Número definido|
>   atualizado"; verde com check quando confirmado; vermelho com X quando inválido.
>   Submeter agora exige confirmar (slug e número). Vale no wizard e no edit-form
>   (valores já gravados começam confirmados).
> - **Máscara do PhoneInput** voltou com o 9: "61 98123-4567" (default, vale no perfil).
> - **Passo 2 (Headers) , X-Signature explicado de verdade**: deixa claro que o token
>   NÃO vai em header; ele é a CHAVE do HMAC. Adicionado bloco "Como gerar a
>   X-Signature" (3 passos) + exemplo de código JavaScript copiável. X-Timestamp
>   explica a janela de 5 min (anti-replay).

## RESOLVIDO , 3ª rodada de feedback (2026-06-17 ~23h40, 4 imagens)
> tsc 0, eslint 0, jest 96 (webhooks+whatsapp) verdes.
> - **Número da empresa = PhoneInput do perfil** (bandeira + DDI + máscara), reusado
>   no wizard e no edit-form. `business_id` agora gravado como dígitos do número
>   internacional (DDI+nacional, sem "+"); card formata via `formatE164ForDisplay`.
>   Edição parseia o valor salvo com `splitE164`.
> - **Máscara/placeholder do PhoneInput** trocada de "11 99123-4567" para
>   "61 8123-4567" (default do componente, vale também no perfil).
> - **Feedback de validação em tempo real** (vermelho abaixo do campo) no Endereço
>   (URL) e no Número da empresa + **botão "certinho"** (`FieldValidateButton`): roxo
>   enquanto inválido (clicar revela o erro), verde quando válido. `step2Valid`/`valid`
>   passam a exigir slug válido e telefone válido (10-11 dígitos BR).
> - **Helper `validateNationalPhone`** exportado de `countries.ts` (reuso).
> - **Ajuda "Como montar o payload"**: Passo 1 com destaque ao método POST; Passo 2
>   reescrito (sem menção a n8n, X-Signature objetivo: HMAC-SHA256 de
>   `${timestamp}.${body}` com o token como chave; mais espaçamento); Passo 3
>   renomeado de "Campos do body" para **"Body"**.

## RESOLVIDO , 2ª rodada de feedback (2026-06-17 ~23h15, 3 imagens)
> Redesign profundo do `whatsapp-inbound-help.tsx` + travas. tsc 0, eslint 0, jest verde.
> - **Número da empresa travado em dígitos**: `onChange` agora faz `.replace(/\D/g, "")`
>   no wizard e no edit-form (antes aceitava texto/caractere especial).
> - **Passo 1 (URL) com estado vazio**: sem slug, mostra aviso âmbar "Preencha o campo
>   Endereço (URL) acima" e o botão **Copiar URL fica desabilitado** (ícone incluso);
>   só habilita quando a URL está preenchida.
> - **Passo 2 (Headers) reescrito para leigo**: cada header (Content-Type/X-Timestamp/
>   X-Signature) com ícone, badge (obrigatório/fixo) e explicação curta; nota destacando
>   que o **token = secret** do webhook e que **o n8n calcula a X-Signature sozinho**
>   (HMAC-SHA256 de `${timestamp}.${body}`, janela 5 min). Conferido contra `hmac.ts`.
> - **Passo 3 renomeado** para **"Campos do body"** (era "Campos do corpo").
> - **Exemplos**: removidos os JSONs soltos; agora **2 cURL COMPLETOS** ("mensagem de
>   texto" e "mensagem de mídia"), cada um URL+headers+body, com opcionais marcados
>   "(opcional)" (visual; copiado fica limpo). Seção renomeada para **"Exemplos de cURL"**.
> - **Dropdown por passo**: cada passo (1/2/3 + Exemplos) é colapsável individual;
>   "Exemplos de cURL" fecha por padrão para a tela não ficar gigante.

## RESOLVIDO , feedback do usuário (2026-06-17 ~23h, com as 4 imagens reenviadas)
> Os 3 ajustes abaixo foram aplicados nesta sessão. tsc 0, eslint 0, jest verdes.
> 1. **`whatsapp-inbound-help.tsx` refeito**: título "Como montar o payload";
>    layout em passos com ícones (Passo 1 Endereço/URL REAL = base+slug copiável,
>    Passo 2 Headers copiáveis, Passo 3 tabela Campo/Obrigatório/Descrição) + seção
>    Corpo (body) com **cURL completo copiável** (URL+headers+body) e exemplos texto/mídia.
>    Campos opcionais marcados com "(opcional)" ao lado do valor (marca só visual; o
>    JSON copiado fica válido). Recebe `inboundBaseUrl`+`path` do wizard e do edit-form.
> 2. **Bug de navegação resolvido**: breadcrumb "Tipo de webhook" virou botão com
>    `onClick` que reseta o wizard (remount via `key={resetKey}` + `setKind(null)`),
>    igual ao "Voltar". `Breadcrumb` agora aceita `onClick` no item.
> 3. **Cor das tags = cor do tipo**: `KindMeta.accent.badge` (verde/sky/violet) +
>    helper `webhookKindBadgeClass(kind)`; aplicado no titleAccessory do cabeçalho de
>    criar (`webhook-create-client`) e de editar (`[id]/editar/page`). Antes fixo roxo.

## (histórico) PENDENTE , feedback do usuário aplicado em (2026-06-17 ~22h50)
1. **"Como enviar os dados" (componente `whatsapp-inbound-help.tsx`) , refazer mais bonito:**
   - Trocar o TÍTULO: não é "Como enviar os dados", e sim algo como **"Como montar o payload"**.
   - Layout em **passos com ícones**: **Passo 1** (URL), espaço, **Passo 2** (Headers),
     espaço, **Passo 3** (a tabela de campos , colunas Campo / Obrigatório / Descrição),
     e mais abaixo o **corpo (body)**. Visual bem cuidado ("bonitinho no código").
   - **MOSTRAR A URL de verdade** (o endpoint = base + slug). Hoje só diz "o endereço
     que você definiu acima" , o usuário quer a URL concreta. Precisa receber o
     `inboundBaseUrl` + `path` (passar do wizard/edit-form para o help).
   - **Copiar cURL completo**: um bloco copiável com o `curl` montado (URL + headers +
     body), além dos JSONs. Headers exibidos "bonitinhos" para copiar.
   - No **JSON de mídia**, marcar os campos opcionais com **"(opcional)"** ao lado do
     valor, DENTRO do exemplo (ex.: `"id": "...", (opcional)`). Vale para `media.id`,
     `media.sha256`, `media.filename`. (Decidir como manter copiável , talvez anotação
     estilizada fora do valor, ou aceitar que o copiado inclua a marca.)
2. **Bug de navegação:** clicar em **"Tipo de webhook"** no breadcrumb NÃO retorna para
   a tela de seleção (passo 1) como o botão "Voltar" faz. Precisa funcionar , resetar
   `kind`/`step` para 1. (Hoje é um `<Link href="/novo">`; provavelmente não reseta o
   estado do wizard. Ver `webhook-create-client.tsx` + o `step`/`kind` do wizard.)
3. **Cor das tags = cor do tipo (consistência):** a tag do tipo no cabeçalho (e o
   `KindBanner`) está sempre ROXA. Tem que seguir a cor do componente/tipo:
   **verde** (Receber mensagens do WhatsApp), **azul/sky** (Receber eventos),
   **roxo/violet** (Enviar eventos). Aplicar em TODA tag/badge do tipo (cabeçalho,
   banner, e onde mais aparecer). O `KindMeta.accent` já tem as cores , reusar.

## UI FEITA (2026-06-17 noite, telas cheias)
- [x] **Telas cheias** de criar (`/integracoes/webhooks/novo`) e editar
  (`/integracoes/webhooks/[id]/editar`) , navegação, NÃO modal. Botões/edição da
  lista navegam (router.push). `WebhookEditForm` reusável; `getWebhook(id)` action.
- [x] **Seletor de eventos por blocos** plugado (saída) no wizard e na edição.
- [x] **Recebe dados do WhatsApp** (toggle) + **número da empresa** (`business_id`,
  obrigatório/único) + **descrição** nas duas telas.
- [x] **Ajuda na tela** (`WhatsappInboundHelp`): tabela de campos + JSON copiável
  (texto e mídia) quando WhatsApp.
- [x] **Card** com tag "WhatsApp · <número>" + descrição.
- [x] Dev no localhost:3000 servindo as telas (200); tsc 0, eslint 0, jest 3148 passed.
- [ ] **Pendente real:** e2e quando o n8n/Meta estiver ligado (disparar contra
  `/api/hooks/<slug>` com HMAC). Limpeza opcional: remover `webhook-edit-dialog.tsx`
  (modal antigo, agora sem uso) e o path legado `/api/integrations/whatsapp/inbound`.

## (UI , plano antigo, agora EXECUTADO)
- [x] **Telas cheias** (navegação) de criar/editar webhook (substituir os modais
  `webhook-wizard.tsx`/`webhook-edit-dialog.tsx`): tipo (enviar/receber); em
  receber → toggle "Recebe dados do WhatsApp" → slug (único), número da empresa
  (business_id, único), descrição, secret. Saída → `WebhookEventSelector`
  (já pronto). ui-ux-pro-max inline.
- [ ] **Ajuda na tela** (quando WhatsApp): tabela de campos + JSON copiável (texto
  e mídia), usando o contrato de `inbound-payload.ts`.
- [ ] **Card do webhook**: tag "WhatsApp" + número da empresa; descrição visível.
- [ ] Garantir dev no localhost:3000 com a versão final + verificação tsc/eslint/jest.

## (antigo plano abaixo, já executado)
- [x] **Contrato de entrada** (`inbound-payload.ts`): renomear `from`→`wa_id`,
  `messageId`→`message_id`, `contactName`→`contact_name`, `phoneNumberId` removido;
  add `user_id` (req), `media{url,mime_type,filename,id,sha256}` (req em mídia),
  `type` ampliado. Zod superRefine: text req em text/audio; media req em mídia.
  CASCATA: `route.ts`, `resolve.ts` (por wa_id), `processor.ts`/`AgentJobData`,
  e os testes (`inbound-payload.test.ts`, `route.test.ts`, `resolve.test.ts`,
  `processor.test.ts`).
- [ ] **Roteamento por slug**: rota genérica de entrada (ex.: `/api/hooks/[...slug]`)
  que acha o webhook pelo `path`, valida `isWhatsappReceiver`+`enabled`, valida HMAC
  com o secret DELE, anexa o `business_id` do webhook, e enfileira pro agente (reusa
  o pipeline do inbound atual). Anexar `business_id` ao envelope de saída p/ o n8n
  rotear. (O endpoint fixo `/api/integrations/whatsapp/inbound` pode virar legado.)
- [ ] **UI tela cheia** (ui-ux-pro-max, inline): páginas de criar/editar webhook
  (substituem o modal `webhook-wizard`/`webhook-edit-dialog`). Tipo (enviar/receber),
  e em receber → toggle "Recebe dados do WhatsApp" → campos slug (único), número da
  empresa (único), descrição, secret. Saída → seletor de eventos por blocos.
- [ ] **Ajuda na tela**: tabela de campos + JSON copiável (texto e mídia) no webhook WhatsApp.
- [ ] **Card do webhook**: tag "WhatsApp" + número da empresa; descrição visível.
- [ ] Verificação: tsc + eslint + jest + (e2e quando o n8n estiver ligado).

## Observações
- `business_id` em código = campo Prisma `businessId` mapeado p/ coluna `business_id`.
- Migration: `prisma/migrations/20260617210851_f5_1_webhook_whatsapp_receiver_e_descricao`.
- DROP das colunas legadas (F5) segue deferido (RADAR `R-f5-drop-booleans-legados`).
