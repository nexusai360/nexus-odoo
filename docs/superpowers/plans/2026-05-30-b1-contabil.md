# PLAN , B1 Contábil (movimento): execução

> **Sobre a SPEC v3** (`docs/superpowers/specs/2026-05-30-b1-contabil-spec.md`).
> **Versão:** v-final (2026-05-30), review adversarial aplicada
> (`reviews/2026-05-30-b1-contabil-plan-reviews.md`, B1P-1..B1P-11). Correções-chave: migration
> via workaround de drift (caminho primário, espelha O1/O3/O4); BI_SCHEMA_REFERENCE é
> OBRIGATÓRIO (trava teste) e acoplado ao schema; Task 5 quebrada em 5a/5b; ordem
> cabeçalho→item no FATO_BUILDERS; injeção explícita do `_RESPOSTA` honesto; `groupBy` fixado.
> Padrão idêntico a O1/O3/O4. Tasks atômicas, TDD onde há lógica. Sem placeholders.
> **Modelo de execução:** inline na sessão principal (Opus), CLAUDE.md §6[8].

## Invariantes (não violar)
- **Aditivo (P1):** nenhuma tool/fato/migration existente é alterada de forma destrutiva.
- **Padrão único de output:** `withFreshness` (`preparando|ok|vazio`). Sem `sem_dado`.
- **Honestidade:** tools de gestão respondem `vazio`+_RESPOSTA enquanto o fato tem 0 linhas.
- **Migration:** roda no Postgres dev COMPARTILHADO , **AVISAR o usuário antes** e usar o
  workaround de drift (criar a migration com `--create-only` + `migrate deploy`, ou aplicar o
  SQL manual) se `migrate dev` pedir reset. Não começar com contexto curto.

---

## Task 0 , Inspeção de fixação (sem código)
- [ ] Reler o builder `src/worker/fatos/fato-dfe.ts` (helpers `relId/relNome`, `dt`, `numStr`,
      `markFatoBuilt`, delete+createMany em `$transaction`) e `fato-conta-contabil.ts` (mapper
      hierárquico). Confirmar nomes Prisma gerados de `RawContabilContaReferencial`.
- [ ] Confirmar `mcp/lib/freshness.ts`: assinatura de `withFreshness`, formato de `FATO_FONTE`,
      como `isVazio`/`extractFirstArray` decide "vazio".
- [ ] Confirmar `mcp/tools/contabil/plano-de-contas.ts` como template de tool (envelope union,
      `enriquecerEnvelope`).
- [ ] Confirmar `src/lib/reports/queries/contabil.ts` (query existente `queryPlanoDeContas`) ,
      o query layer B1 estende este arquivo.
- **Saída:** nenhuma dúvida de assinatura. Sem commit.

## Task 1 , Schema Prisma: 3 fatos + 2 raws novos
> O raw do referencial JÁ existe (`RawContabilContaReferencial`). Criar os 2 raws de
> lançamento/item + os 3 fatos.
- [ ] Em `prisma/schema.prisma`, adicionar (padrão dos raws/fatos existentes, `@@map` snake):
  - `RawContabilLancamento` (`raw_contabil_lancamento`: odooId PK, data Json, odooWriteDate,
    syncedAt, rawDeleted + 2 índices) , idêntico ao `RawSpedConsultaDfeItem`.
  - `RawContabilLancamentoItem` (`raw_contabil_lancamento_item`) , idem.
  - `FatoContabilContaReferencial` (`fato_contabil_conta_referencial`): odooId PK, codigo,
    nome, nomeCompleto?, natureza?(String), tipo?(String), nivel?(Int), parentPath?,
    contaSuperiorId?(Int), atualizadoEm. Índices: `natureza`, `codigo`.
  - `FatoContabilLancamento` (`fato_contabil_lancamento`): odooId PK, codigo?, tipo?,
    dataLancamento?(DateTime), valor/valorDebito/valorCredito (Decimal(18,2) default 0),
    empresaId?(Int), atualizadoEm. Índice: `dataLancamento`.
  - `FatoContabilLancamentoItem` (`fato_contabil_lancamento_item`): odooId PK, lancamentoId?(Int),
    contaId?(Int), contaCodigo?, contaNome?, contaNatureza?(String), centroCustoId?(Int),
    centroCustoNome?, natureza?(String D/C), valor/valorDebito/valorCredito (Decimal(18,2)
    default 0), lancamentoTipo?, dataLancamento?(DateTime), historico?, atualizadoEm.
    Índices: `dataLancamento`, `contaId`, `contaNatureza`, `centroCustoId`, `lancamentoId`.
- [ ] `npx prisma format` , valida o schema.
- [ ] **OBRIGATÓRIO no MESMO passo (B1P-6):** adicionar à constante `BI_SCHEMA_REFERENCE` em
      `src/lib/agent/bi-schema-reference.ts` três blocos DDL no formato existente (ver `fato_dfe`
      L263, `fato_conta_contabil` L304): `TABLE fato_contabil_conta_referencial(...)`,
      `TABLE fato_contabil_lancamento(...)`, `TABLE fato_contabil_lancamento_item(...)`. O teste
      `bi-schema-reference.test.ts` varre todo `@@map("fato_*")` do schema e EXIGE cada nome na
      constante , adicionar os 3 `model Fato*` sem isso QUEBRA a suíte. Nomes têm que bater
      exatamente com os `@@map`.
- **Verificação:** `npx prisma validate`; `npx jest bi-schema-reference` verde. **Sem migration ainda.**
- **Commit:** `feat(b1): schema 3 fatos contabeis + 2 raws + BI_SCHEMA_REFERENCE`.

## Task 2 , Migration aditiva (AVISAR ANTES , workaround de drift é o caminho PRIMÁRIO)
> **B1P-5:** o Postgres dev compartilhado TEM drift pré-existente conhecido (O1/O3/R2-ctx
> aplicaram via `$executeRawUnsafe`/`db execute` + `migrate resolve --applied`, NÃO `migrate
> deploy`). Espelhar o que funcionou.
- [ ] **AVISAR o usuário** que vai rodar migration no Postgres dev compartilhado.
- [ ] `npx prisma migrate dev --name b1_contabil_movimento --create-only` (gera a pasta de
      migration com o SQL, SEM aplicar) , revisar que é só `CREATE TABLE`/`CREATE INDEX`
      (aditivo, 5 tabelas: 2 raw + 3 fato).
- [ ] Aplicar o SQL direto (workaround drift): `npx prisma db execute --file
      prisma/migrations/<ts>_b1_contabil_movimento/migration.sql --schema prisma/schema.prisma`.
- [ ] `npx prisma migrate resolve --applied <ts>_b1_contabil_movimento` (marca como aplicada
      sem reset). `npx prisma generate`.
- **Verificação:** as 5 tabelas existem (`prisma db execute` com `SELECT to_regclass(...)` ou
      `\dt`); `npx prisma migrate status` não pede reset.
- **Commit:** `feat(b1): migration b1_contabil_movimento (3 fatos + 2 raws, aditiva, via workaround drift)`.

## Task 3 , MODEL_CATALOG: 2 raws novos + teste
- [ ] `src/worker/catalog/model-catalog.ts`: adicionar `{ odooModel: "contabil.lancamento",
      mode: "incremental" }` e `{ odooModel: "contabil.lancamento.item", mode: "incremental" }`
      (comentário `// B1 (onda contábil)`). O referencial já está lá.
- [ ] `src/worker/catalog/model-catalog.test.ts`: `toHaveLength(114)`→**116** (confirmar via
      `grep -c '{ odooModel:'` que parte de 114 e que o referencial NÃO é recontado , B1P-1);
      adicionar `const MODELOS_B1 = new Set(["contabil.lancamento","contabil.lancamento.item"])`
      e incluí-lo no filtro `noCatalogo` (padrão `MODELOS_O1`).
- [ ] **B1P-8:** verificar se `contabil.conta.referencial` (já no catálogo, sem arquivo em
      `discovery/output/modelos`) já está em algum set de exceção; se não estiver e o teste de
      "cobre exatamente os modelos de discovery" rodar em dev, incluí-lo também no `MODELOS_B1`.
- **Verificação:** `npx jest model-catalog` verde.
- **Commit:** `feat(b1): registra raws contabeis de lancamento no MODEL_CATALOG`.

## Task 4 , Builder do fato referencial (TDD) , o validável real
- [ ] `src/worker/fatos/fato-contabil-conta-referencial.ts`: `mapContaReferencialRow(raw)` puro
      (codigo, nome, nomeCompleto, natureza, tipo, nivel, parentPath,
      contaSuperiorId=relId(conta_superior_id)) + `rebuildFatoContabilContaReferencial(prisma)`
      (lê `rawContabilContaReferencial` where rawDeleted=false; delete+createMany; markFatoBuilt).
- [ ] Teste pareado `fato-contabil-conta-referencial.test.ts` com mock realista (amostra real:
      `{codigo:"1",nome:"ATIVO",natureza:"01",tipo:"A",nivel:1,parent_path:"1/"}`).
- **Verificação:** `npx jest fato-contabil-conta-referencial` verde.
- **Commit:** `feat(b1): builder fato_contabil_conta_referencial (de-para SPED, dado real)`.

## Task 5a , Builder do cabeçalho (TDD, estrutural)
- [ ] `src/worker/fatos/fato-contabil-lancamento.ts`: `mapContabilLancamentoRow` puro (codigo,
      tipo, dataLancamento=dt(data_lancamento), valor/valorDebito/valorCredito=Number(?? 0),
      empresaId=relId(empresa_id)) + `rebuildFatoContabilLancamento` (delete+createMany+markFatoBuilt).
- [ ] Teste pareado com mock.
- **Verificação:** `npx jest fato-contabil-lancamento.test` verde.
- **Commit:** `feat(b1): builder fato_contabil_lancamento (cabecalho, estrutural)`.

## Task 5b , Builder do item/partida (TDD, estrutural , o join é o ponto de risco)
- [ ] `src/worker/fatos/fato-contabil-lancamento-item.ts`: `mapContabilLancamentoItemRow` puro
      (lancamentoId=relId(lancamento_id), contaId=relId/contaNome=relNome(conta_id),
      centroCustoId/Nome=relId/relNome(centro_custo_id), natureza=str, valor/valorDebito/
      valorCredito=Number(??0), dataLancamento=dt, historico=str(historico_completo)).
- [ ] `rebuildFatoContabilLancamentoItem(prisma)`: ANTES do map, montar 2 mapas auxiliares:
      `contaNatureza` de `Map<contaOdooId,natureza>` lendo `prisma.fatoContaContabil.findMany(
      {select:{odooId:true,natureza:true}})` (B1P-4: `conta_id` do item → odooId casa com a chave);
      `lancamentoTipo` de `Map<lancamentoOdooId,tipo>` lendo `fatoContabilLancamento`. O mapper
      recebe os mapas e preenche `contaNatureza`/`lancamentoTipo`. Anotar `// CONFIRMAR na
      ativação` em valor vs valorDebito/credito e na dependência de `fato_conta_contabil` populado.
- [ ] Teste pareado: mapper puro + rebuild com mapas mockados (provar o join natureza/tipo).
      Com 0 registros reais, o teste prova a LÓGICA, não números de produção.
- **Verificação:** `npx jest fato-contabil-lancamento-item.test` verde.
- **Commit:** `feat(b1): builder fato_contabil_lancamento_item (join contaNatureza+lancamentoTipo)`.

## Task 6 , Registry + FATO_FONTE
- [ ] `src/worker/fatos/registry.ts`: import dos 3 builders + 3 entradas em `FATO_BUILDERS`
      (`cycle:"incremental"`, comentário `// B1 (onda contábil)`). **Ordem obrigatória (B1P-4):**
      `fato_contabil_lancamento` ANTES de `fato_contabil_lancamento_item` (o item lê o mapa de
      tipo do cabeçalho já buildado). Referencial pode vir antes de ambos.
- [ ] `mcp/lib/freshness.ts`: 3 entradas em `FATO_FONTE`
      (`fato_contabil_conta_referencial`→contabil.conta.referencial;
      `fato_contabil_lancamento`→contabil.lancamento; `fato_contabil_lancamento_item`→
      contabil.lancamento.item; todas `incremental`).
- **Verificação:** `npx tsc --noEmit`; `npx jest registry freshness` verde (se houver testes).
- **Commit:** `feat(b1): registra 3 fatos contabeis em FATO_BUILDERS e FATO_FONTE`.

## Task 7 , Query layer (TDD)
> **B1P-9:** o `contabil.ts` existente (`queryPlanoDeContas`/`queryEstruturaConta`) usa
> `findMany`+`count`. As agregações novas usam `prisma.fatoContabilLancamentoItem.groupBy(
> {by:[...], _sum:{valorDebito:true, valorCredito:true}})` (padrão dos query layers
> financeiro/dfe), depois mapeia para o shape de saída. Especificar groupBy, não findMany+reduce.
- [ ] Estender `src/lib/reports/queries/contabil.ts` com:
  - `queryContabilSaldoConta(prisma, {periodoDe?,periodoAte?,contaCodigo?,limite?})` →
    agrupa item por conta, `debito=ΣvalorDebito`, `credito=ΣvalorCredito`, `saldo=debito−credito`.
  - `queryContabilMovimentoConta(prisma, {contaCodigo,periodoDe?,periodoAte?,limite?})` →
    partidas (razão) de uma conta, ordenadas por dataLancamento.
  - `queryContabilResultadoPorNatureza(prisma, {periodoDe?,periodoAte?})` → filtra
    `contaNatureza='04'` e `lancamentoTipo != 'E'`; receita=Σ onde natureza='C',
    despesa=Σ onde natureza='D', resultado=receita−despesa.
  - `queryContabilCentroCusto(prisma, {periodoDe?,periodoAte?,limite?})` → agrupa por
    centroCustoId/Nome, saldo por centro.
  - `queryContabilContaReferencial(prisma, {termo?,natureza?,limite?})` → lista referencial
    (padrão `queryPlanoDeContas`), filtro por termo (codigo/nome) e natureza.
  - Helper `fatoContabilItemCount(prisma)` → `prisma.fatoContabilLancamentoItem.count()`
    (para o _RESPOSTA distinguir "não operado" de "filtro vazio").
- [ ] Testes pareados `contabil.test.ts` com mocks (saldo/resultado/centro com linhas
      sintéticas; referencial com dado mock). Provar a aritmética (Σdéb−Σcréd, receita−despesa).
- **Verificação:** `npx jest queries/contabil` verde.
- **Commit:** `feat(b1): query layer contabil (saldo, razao, resultado, centro custo, referencial)`.

## Task 8 , Tool `contabil_conta_referencial` (a real)
- [ ] `mcp/tools/contabil/conta-referencial.ts`: `ToolEntry` id `contabil_conta_referencial`,
      dominio `contabil`, descricao distinguindo do plano de contas da empresa ("plano
      REFERENCIAL SPED / de-para fiscal, distinto do plano de contas da empresa"). Handler:
      `withFreshness(prisma,["fato_contabil_conta_referencial"],()=>queryContabilContaReferencial)`
      + `enriquecerEnvelope`. Output union padrão.
- [ ] Registrar em `mcp/tools/contabil/index.ts`.
- **Verificação:** `npx tsc --noEmit`.
- **Commit:** `feat(b1): tool contabil_conta_referencial (dado real)`.

## Task 9 , Tools de gestão (4) com honestidade data-driven
> **Contrato sem ambiguidade (B1P-3):**
> 1. `outputSchema.dados` de cada tool inclui `_RESPOSTA/_listaTruncada/_DESTAQUE/_agregado`
>    opcionais (copiar de `plano-de-contas.ts` L31-34) e a union `preparando | {ok|vazio}`.
> 2. Handler: `const total = await fatoContabilItemCount(prisma); const env = await
>    withFreshness(prisma,["fato_contabil_lancamento_item"],()=>queryX(...));`
>    `if (env.estado==="preparando") return env;`
>    `const out = enriquecerEnvelope(env, "<tool_id>", {destaque:{...}, agregado:{...}});`
> 3. **Injeção do `_RESPOSTA` honesto:** como não há formatador por-tool registrado para estas
>    tools novas, o `enriquecerEnvelope` usaria o genérico , então **sobrescrever após**:
>    `if (out.estado==="vazio") out.dados._RESPOSTA = total===0 ? "A contabilidade ainda não é
>    operada no Odoo (sem lançamentos); esta consulta responde quando os lançamentos forem
>    lançados." : "Sem lançamentos nesse recorte (conta/período).";`
>    `return out;` (mutação válida: `withFreshness` devolve `dados` em ok/vazio , B1P-3).
- [ ] `mcp/tools/contabil/saldo-conta.ts` → `contabil_saldo_conta`.
- [ ] `mcp/tools/contabil/movimento-conta.ts` → `contabil_movimento_conta`.
- [ ] `mcp/tools/contabil/resultado-por-natureza.ts` → `contabil_resultado_por_natureza`.
- [ ] `mcp/tools/contabil/centro-custo.ts` → `contabil_centro_custo`.
- [ ] Registrar as 4 em `mcp/tools/contabil/index.ts` (total do índice: 7).
- **Verificação:** `npx tsc --noEmit`.
- **Commit:** `feat(b1): 4 tools contabeis de gestao (saldo/razao/resultado/centro), honestas data-driven`.

## Task 10 , Bumps de teste + snapshot + vocab
> BI_SCHEMA_REFERENCE já foi feito na Task 1 (trava de teste). Aqui ficam contagem, vocab, snapshot.
- [ ] `mcp/__tests__/integration.test.ts`: `CONTABIL_IDS` += os 5 ids (isso já flui para
      `TODOS_IDS` via spread `...CONTABIL_IDS`, então os `toEqual([...TODOS_IDS])` das linhas
      ~217/559 passam automaticamente , B1P-2). Bumpar os hardcoded: `74`→`79` (linhas ~214,
      251, 259, 556); `83`→`88` (linha 237 + comentário); "admin vê as 2 tools de contábil"→7
      (linha ~340); corrigir texto cosmético "47 tools" (linha 542).
- [ ] `src/lib/agent/router/domain-vocabulary.ts`: enriquecer description/examples do domínio
      `contabil` (saldo/razão/resultado/centro de custo). `DOMAINS.length` continua 9 (invalida
      `VOCABULARY_VERSION` → rebuild do `app`).
- [ ] `npm run gen:mcp-catalog` (não requer DB/env , B1P-7) , regenera
      `src/lib/mcp-catalog-snapshot.json`.
- **Verificação:** `npx jest integration domain-vocabulary` verde; `npx tsc --noEmit`.
- **Commit:** `test(b1): bumps de contagem (74->79, 83->88) + vocab Router + snapshot do catalogo`.

## Task 11 , Verificação completa + E2E real do referencial
- [ ] `npx tsc --noEmit` (raiz) e tsc do mcp; `npx eslint src/ mcp/`; `npx jest` (suíte cheia).
- [ ] Rebuild da PASTA PRINCIPAL (runbook `docs/runbooks/sync-novo-fato.md`): da pasta principal
      em `main` após merge OU, para E2E local agora, `docker compose build app` +
      `up -d --force-recreate worker mcp` na worktree (avaliar; o E2E do referencial não exige
      container, pode rodar via script tsx contra o cache).
- [ ] **E2E real do referencial:** sincronizar `contabil.conta.referencial` (já no catálogo) e
      rodar `rebuildFatoContabilContaReferencial`; conferir contra produção:
      `fato_contabil_conta_referencial` ≈ 2216 linhas, DIST natureza `01≈948,02≈376,03≈120,04≈772`,
      e a tool `contabil_conta_referencial` retorna `estado:"ok"` com dado coerente.
- [ ] Conferir que `contabil_saldo_conta` (e as outras 3) retornam `estado:"vazio"` + _RESPOSTA
      "não operado" (fato item com 0 linhas) , honestidade comprovada.
- [ ] Painel "Estado da ingestão": `contabil.lancamento`/`.item` aparecem com status ok / 0 reg.
- **Commit:** `test(b1): E2E real do referencial (2216) + honestidade vazio das tools de gestao`.

## Task 12 , Code review + checklist de ativação + PR
- [ ] `/gsd-code-review` na onda; aplicar achados materiais.
- [ ] Anexar o checklist de ativação (SPEC §4) ao PR.
- [ ] Abrir PR (gated): título + body com escopo, evidências (tsc/eslint/jest, E2E referencial,
      honestidade vazio), e o checklist de ativação. **Declarar a divergência consciente do
      plano-mãe §0.1 (B1P-11):** tools Balde B aqui são VISÍVEIS+honestas (`_RESPOSTA` "não
      operado"), não ocultas , o mecanismo de ocultação não existe no `ToolEntry` e seria
      mudança de RBAC; decisão registrada na SPEC v3 §0/§2.2. **Merge é decisão do usuário.**

## Ordem e dependências
0→1 (schema+BI ref)→2 (migration, AVISAR)→3 (catálogo)→4 (referencial)→5a (cabeçalho)→5b (item,
depende de 5a p/ ordem)→6→7→8→9→10→11→12. Task 4 é independente de 5a/5b. Migration (Task 2) é o
ponto de atenção (AVISAR).

## Bateria R-X (gate herdado, pós-merge)
Como nas ondas anteriores: rodar a bateria R-X contra a main mergeada, com perguntas
contábeis novas. Separado do E2E estrutural. Pendente para o conjunto das ondas.
