# F4 L1c — Resíduo operacional 4a — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sincronizar os três modelos operacionais do resíduo 4a do censo (`sped.certificado`, `finan.baixa.lancamento`, `pedido.faturamento`) para o cache, com a tool `fiscal_certificados` lendo `fato_certificado`.

**Architecture:** Reusa os padrões da L1a: modelo Prisma `Raw*` por modelo Odoo, entrada em `MODEL_CATALOG`, migration por onda, fato com builder transacional, query framework-neutra, `ToolEntry` com `withFreshness`. Acrescenta um único mecanismo novo no sync: exclusão de campos por modelo (`excludeFields`), para não copiar `senha`/`arquivo` de `sped.certificado` ao cache.

**Tech Stack:** TypeScript, Prisma v7, Postgres, `@modelcontextprotocol/sdk`, Zod, Jest.

**Spec:** `docs/superpowers/specs/2026-05-22-f4-l1b-l1c-residuo-spec.md` (v3).

---

## Ordem de execução geral

Este plano cobre **apenas a onda L1c**. A sequência completa pedida é:

1. **L1b** — executar `T6.1`–`T6.10` do plano-mãe (`docs/superpowers/plans/2026-05-21-f4-leitura-expansao.md`). Migration `f4l_referencia`.
2. **L1c** — este plano (Tasks 1–11). Migration `f4l_residuo_4a`.
3. **Onda I** — executar `TI.1`–`TI.7` do plano-mãe. Ingestão real e conferência de contagem.

L1c é independente de L1b no código (toca arquivos diferentes); a ordem L1b→L1c é só para agrupar as migrations. Pode-se executar L1c sem L1b se necessário.

---

## File Structure

**Criar:**
- `src/worker/fatos/fato-certificado.ts` — builder de `fato_certificado`.
- `src/worker/fatos/fato-certificado.test.ts` — teste do builder.
- `mcp/tools/fiscal/certificados.ts` — tool `fiscal_certificados`.
- `prisma/migrations/<timestamp>_f4l_residuo_4a/migration.sql` — gerada pelo Prisma.

**Modificar:**
- `src/worker/catalog/model-catalog.ts` — tipo `CatalogEntry` ganha `excludeFields`; 3 entradas novas.
- `src/worker/odoo/field-selection.ts` — `getModelFields` subtrai `excludeFields`.
- `src/worker/odoo/field-selection.test.ts` — teste da exclusão.
- `prisma/schema.prisma` — 3 modelos `Raw*` + `FatoCertificado`.
- `src/worker/fatos/registry.ts` — registra o builder.
- `mcp/lib/freshness.ts` — `FATO_FONTE` ganha `fato_certificado`.
- `src/lib/reports/queries/fiscal-complementar.ts` — `queryCertificados`.
- `src/lib/reports/queries/fiscal-complementar.test.ts` — teste de `queryCertificados`.
- `mcp/tools/fiscal/index.ts` — exporta a tool nova.
- `mcp/__tests__/integration.test.ts` — catálogo 45→46; `FISCAL_IDS` ganha `fiscal_certificados`.
- `src/lib/mcp-catalog-snapshot.json` — regenerado por `npm run gen:mcp-catalog`.
- `src/lib/agent/bi-schema-reference.ts` — bloco `TABLE fato_certificado`.

---

## Task 1: `excludeFields` no catálogo e em `getModelFields`

**Files:**
- Modify: `src/worker/catalog/model-catalog.ts`
- Modify: `src/worker/odoo/field-selection.ts`
- Test: `src/worker/odoo/field-selection.test.ts`

- [ ] **Step 1: Estender o tipo `CatalogEntry`**

Em `src/worker/catalog/model-catalog.ts`, adicionar o campo opcional à interface:

```typescript
export interface CatalogEntry {
  odooModel: string;
  mode: SyncMode;
  /** Campos que NÃO devem ser copiados para o cache (segredos, blobs).
   * `getModelFields` os subtrai. Vazio/ausente = comportamento padrão. */
  excludeFields?: readonly string[];
}
```

- [ ] **Step 2: Escrever o teste falho da exclusão**

Em `src/worker/odoo/field-selection.test.ts`, adicionar:

```typescript
it("subtrai os excludeFields declarados no MODEL_CATALOG", async () => {
  // sped.certificado declara excludeFields: ["senha", "arquivo"]
  const client = {
    executeKw: jest.fn().mockResolvedValue({
      id: { type: "integer", store: true },
      tipo: { type: "selection", store: true },
      senha: { type: "char", store: true },
      arquivo: { type: "binary", store: true },
      proprietario: { type: "char", store: true },
    }),
  } as unknown as OdooClient;

  clearFieldCache();
  const fields = await getModelFields(client, "sped.certificado");

  expect(fields).toContain("tipo");
  expect(fields).toContain("proprietario");
  expect(fields).not.toContain("senha");
  expect(fields).not.toContain("arquivo");
});
```

- [ ] **Step 3: Rodar o teste e confirmar que falha**

Run: `npx jest src/worker/odoo/field-selection.test.ts -t "subtrai os excludeFields"`
Expected: FAIL — hoje `getModelFields` não conhece `excludeFields`; `senha`/`arquivo` aparecem.

- [ ] **Step 4: Implementar a exclusão em `getModelFields`**

Em `src/worker/odoo/field-selection.ts`, importar o catálogo e subtrair os campos. No topo:

```typescript
import { MODEL_CATALOG } from "../catalog/model-catalog";
```

No fim de `getModelFields`, antes de `cache.set`:

```typescript
  // Exclusão por modelo: remove campos sensíveis/blobs declarados no catálogo.
  const exclude = MODEL_CATALOG.find((e) => e.odooModel === model)?.excludeFields;
  const finalFields = exclude && exclude.length
    ? fields.filter((f) => !exclude.includes(f))
    : fields;

  cache.set(model, finalFields);
  return finalFields;
```

(Substitui o `cache.set(model, fields); return fields;` atual.)

- [ ] **Step 5: Rodar o teste e confirmar que passa**

Run: `npx jest src/worker/odoo/field-selection.test.ts`
Expected: PASS — todos os testes do arquivo, incluindo o novo.

- [ ] **Step 6: Commit**

```bash
git add src/worker/catalog/model-catalog.ts src/worker/odoo/field-selection.ts src/worker/odoo/field-selection.test.ts
git commit -m "feat(f4-l1c): exclusao de campos por modelo no sync (excludeFields)"
```

---

## Task 2: Modelos Prisma `Raw*` e `FatoCertificado` + migration

**Files:**
- Modify: `prisma/schema.prisma`
- Create: `prisma/migrations/<timestamp>_f4l_residuo_4a/migration.sql` (gerada)

- [ ] **Step 1: Adicionar os 3 modelos `Raw*`**

Em `prisma/schema.prisma`, ao lado dos demais `Raw*` (formato idêntico a `RawSpedCartaCorrecao`):

```prisma
model RawSpedCertificado {
  odooId        Int       @id @map("odoo_id")
  data          Json
  odooWriteDate DateTime? @map("odoo_write_date")
  syncedAt      DateTime  @default(now()) @map("synced_at")
  rawDeleted    Boolean   @default(false) @map("raw_deleted")

  @@index([odooWriteDate])
  @@index([rawDeleted])
  @@map("raw_sped_certificado")
}

model RawFinanBaixaLancamento {
  odooId        Int       @id @map("odoo_id")
  data          Json
  odooWriteDate DateTime? @map("odoo_write_date")
  syncedAt      DateTime  @default(now()) @map("synced_at")
  rawDeleted    Boolean   @default(false) @map("raw_deleted")

  @@index([odooWriteDate])
  @@index([rawDeleted])
  @@map("raw_finan_baixa_lancamento")
}

model RawPedidoFaturamento {
  odooId        Int       @id @map("odoo_id")
  data          Json
  odooWriteDate DateTime? @map("odoo_write_date")
  syncedAt      DateTime  @default(now()) @map("synced_at")
  rawDeleted    Boolean   @default(false) @map("raw_deleted")

  @@index([odooWriteDate])
  @@index([rawDeleted])
  @@map("raw_pedido_faturamento")
}
```

- [ ] **Step 2: Adicionar o modelo `FatoCertificado`**

Ao lado de `FatoCartaCorrecao` em `prisma/schema.prisma`:

```prisma
model FatoCertificado {
  odooId             Int       @id @map("odoo_id")
  tipo               String?
  numeroSerie        String?   @map("numero_serie")
  proprietario       String?
  cnpjCpf            String?   @map("cnpj_cpf")
  dataInicioValidade DateTime? @map("data_inicio_validade")
  dataFimValidade    DateTime? @map("data_fim_validade")
  dataVencimentoUtil DateTime? @map("data_vencimento_util")
  nomeArquivo        String?   @map("nome_arquivo")
  atualizadoEm       DateTime  @default(now()) @map("atualizado_em")

  @@index([dataFimValidade])
  @@map("fato_certificado")
}
```

- [ ] **Step 3: Gerar a migration**

Run: `npx prisma migrate dev --name f4l_residuo_4a`
Expected: cria `prisma/migrations/<timestamp>_f4l_residuo_4a/` com `CREATE TABLE` das 4 tabelas; `prisma generate` roda automático.

- [ ] **Step 4: Verificar typecheck**

Run: `npx tsc --noEmit`
Expected: PASS — o client Prisma novo (`rawSpedCertificado`, `fatoCertificado`, etc.) compila.

- [ ] **Step 5: Commit**

```bash
git add prisma/schema.prisma prisma/migrations/
git commit -m "feat(f4-l1c): schema raw das 3 tabelas do residuo 4a + fato_certificado"
```

---

## Task 3: Entradas no `MODEL_CATALOG`

**Files:**
- Modify: `src/worker/catalog/model-catalog.ts`

- [ ] **Step 1: Adicionar as 3 entradas**

Ao fim do array `MODEL_CATALOG` (depois de `sped.carta.correcao`):

```typescript
  { odooModel: "sped.certificado", mode: "incremental", excludeFields: ["senha", "arquivo"] },
  { odooModel: "finan.baixa.lancamento", mode: "incremental" },
  { odooModel: "pedido.faturamento", mode: "incremental" },
```

- [ ] **Step 2: Atualizar o comentário-cabeçalho do catálogo**

O topo do arquivo tem uma linha de contagem (ex.: `// 77 incremental | 5 snapshot | 2 estatico.`). Recontar e atualizar para refletir +3 incremental.

- [ ] **Step 3: Verificar**

Run: `npx tsc --noEmit && npx jest src/worker/catalog`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add src/worker/catalog/model-catalog.ts
git commit -m "feat(f4-l1c): registra os 3 modelos do residuo 4a no MODEL_CATALOG"
```

---

## Task 4: Builder `fato-certificado.ts`

**Files:**
- Create: `src/worker/fatos/fato-certificado.ts`
- Create: `src/worker/fatos/fato-certificado.test.ts`
- Modify: `src/worker/fatos/registry.ts`

- [ ] **Step 1: Escrever o teste falho do mapeamento**

Criar `src/worker/fatos/fato-certificado.test.ts`:

```typescript
import { mapCertificadoRow } from "./fato-certificado";

describe("mapCertificadoRow", () => {
  it("mapeia o JSONB do raw para a linha tipada do fato", () => {
    const raw = {
      id: 25,
      tipo: "A1",
      numero_serie: "7286666072932397959",
      proprietario: "JMF COMERCIO",
      cnpj_cpf: "45.424.185/0001-08",
      data_inicio_validade: "2026-05-12 12:16:06",
      data_fim_validade: "2027-05-12 12:16:06",
      data_vencimento_util: "2027-05-12",
      nome_arquivo: "JMF.pfx",
    };
    const linha = mapCertificadoRow(raw);
    expect(linha.odooId).toBe(25);
    expect(linha.tipo).toBe("A1");
    expect(linha.numeroSerie).toBe("7286666072932397959");
    expect(linha.cnpjCpf).toBe("45.424.185/0001-08");
    expect(linha.dataFimValidade?.toISOString().slice(0, 10)).toBe("2027-05-12");
    expect(linha.nomeArquivo).toBe("JMF.pfx");
  });

  it("tolera campos ausentes (null)", () => {
    const linha = mapCertificadoRow({ id: 1 });
    expect(linha.odooId).toBe(1);
    expect(linha.tipo).toBeNull();
    expect(linha.dataFimValidade).toBeNull();
  });
});
```

- [ ] **Step 2: Rodar o teste e confirmar que falha**

Run: `npx jest src/worker/fatos/fato-certificado.test.ts`
Expected: FAIL — `fato-certificado.ts` não existe.

- [ ] **Step 3: Implementar o builder**

Criar `src/worker/fatos/fato-certificado.ts` (espelha `fato-carta-correcao.ts`):

```typescript
// src/worker/fatos/fato-certificado.ts
// FONTE: raw_sped_certificado (modelo sped.certificado).
// ESCOPO: certificados digitais das empresas. senha/arquivo NÃO são copiados
// (excludeFields no MODEL_CATALOG) — não chegam aqui.
// CYCLE: incremental — volume baixo (~11 certificados).
import type { PrismaClient } from "../../generated/prisma/client";
import { markFatoBuilt } from "./fato-build-state";

export interface FatoCertificadoRow {
  odooId: number;
  tipo: string | null;
  numeroSerie: string | null;
  proprietario: string | null;
  cnpjCpf: string | null;
  dataInicioValidade: Date | null;
  dataFimValidade: Date | null;
  dataVencimentoUtil: Date | null;
  nomeArquivo: string | null;
}

function str(v: unknown): string | null {
  return typeof v === "string" && v.length > 0 ? v : null;
}

function dateOrNull(v: unknown): Date | null {
  if (typeof v !== "string" || v.length < 8) return null;
  const d = new Date(v);
  return Number.isNaN(d.getTime()) ? null : d;
}

export function mapCertificadoRow(raw: Record<string, unknown>): FatoCertificadoRow {
  return {
    odooId: Number(raw.id),
    tipo: str(raw.tipo),
    numeroSerie: str(raw.numero_serie),
    proprietario: str(raw.proprietario),
    cnpjCpf: str(raw.cnpj_cpf),
    dataInicioValidade: dateOrNull(raw.data_inicio_validade),
    dataFimValidade: dateOrNull(raw.data_fim_validade),
    dataVencimentoUtil: dateOrNull(raw.data_vencimento_util),
    nomeArquivo: str(raw.nome_arquivo),
  };
}

/** Reconstrói fato_certificado a partir de raw_sped_certificado. */
export async function rebuildFatoCertificado(prisma: PrismaClient): Promise<number> {
  const rawRows = await prisma.rawSpedCertificado.findMany({
    where: { rawDeleted: false },
  });
  const mapped = rawRows.map((r) =>
    mapCertificadoRow(r.data as Record<string, unknown>),
  );
  await prisma.$transaction(async (tx) => {
    await tx.fatoCertificado.deleteMany({});
    if (mapped.length) {
      await tx.fatoCertificado.createMany({ data: mapped });
    }
    await markFatoBuilt(tx, "fato_certificado");
  });
  return mapped.length;
}
```

- [ ] **Step 4: Registrar o builder em `registry.ts`**

Em `src/worker/fatos/registry.ts`, adicionar o import e a entrada no array `FATO_BUILDERS`:

```typescript
import { rebuildFatoCertificado } from "./fato-certificado";
```

```typescript
  { nome: "fato_certificado", cycle: "incremental", run: rebuildFatoCertificado },
```

- [ ] **Step 5: Rodar o teste e confirmar que passa**

Run: `npx jest src/worker/fatos/fato-certificado.test.ts`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src/worker/fatos/fato-certificado.ts src/worker/fatos/fato-certificado.test.ts src/worker/fatos/registry.ts
git commit -m "feat(f4-l1c): builder fato_certificado"
```

---

## Task 5: `FATO_FONTE` em `freshness.ts`

**Files:**
- Modify: `mcp/lib/freshness.ts`

- [ ] **Step 1: Adicionar a entrada no mapa `FATO_FONTE`**

Em `mcp/lib/freshness.ts`, junto das demais entradas de `FATO_FONTE`:

```typescript
  fato_certificado:          { model: "sped.certificado",          mode: "incremental" },
```

- [ ] **Step 2: Verificar**

Run: `npx tsc --noEmit -p mcp/tsconfig.json && npx jest mcp/lib`
Expected: PASS.

- [ ] **Step 3: Commit**

```bash
git add mcp/lib/freshness.ts
git commit -m "feat(f4-l1c): fato_certificado no mapa FATO_FONTE"
```

---

## Task 6: Query `queryCertificados`

**Files:**
- Modify: `src/lib/reports/queries/fiscal-complementar.ts`
- Test: `src/lib/reports/queries/fiscal-complementar.test.ts`

- [ ] **Step 1: Escrever o teste falho**

Em `src/lib/reports/queries/fiscal-complementar.test.ts`, adicionar (e o import de `queryCertificados`):

```typescript
describe("queryCertificados", () => {
  it("lista certificados ordenados por validade (mais próximo de vencer primeiro)", async () => {
    const mockPrisma = {
      fatoCertificado: {
        findMany: jest.fn().mockResolvedValue([
          {
            odooId: 25, tipo: "A1", numeroSerie: "72866",
            proprietario: "JMF", cnpjCpf: "45.424.185/0001-08",
            dataInicioValidade: new Date("2026-05-12T12:16:06"),
            dataFimValidade: new Date("2027-05-12T12:16:06"),
            dataVencimentoUtil: new Date("2027-05-12T00:00:00"),
            nomeArquivo: "JMF.pfx",
          },
        ]),
        count: jest.fn().mockResolvedValue(1),
      },
    } as unknown as Parameters<typeof queryCertificados>[0];

    const result = await queryCertificados(mockPrisma);
    expect(result.total).toBe(1);
    expect(result.linhas[0]?.proprietario).toBe("JMF");
    expect(result.linhas[0]?.dataFimValidade).toBe("2027-05-12");
    const call = (mockPrisma.fatoCertificado.findMany as jest.Mock).mock.calls[0][0];
    expect(call.orderBy).toEqual({ dataFimValidade: "asc" });
  });
});
```

- [ ] **Step 2: Rodar o teste e confirmar que falha**

Run: `npx jest src/lib/reports/queries/fiscal-complementar.test.ts -t queryCertificados`
Expected: FAIL — `queryCertificados` não existe.

- [ ] **Step 3: Implementar `queryCertificados`**

No fim de `src/lib/reports/queries/fiscal-complementar.ts` (segue o padrão de `queryCartaCorrecao`; `dia()` já existe no arquivo):

```typescript
// ---------------------------------------------------------------------------
// Certificados digitais
// ---------------------------------------------------------------------------

export interface CertificadoLinha {
  odooId: number;
  tipo: string | null;
  numeroSerie: string | null;
  proprietario: string | null;
  cnpjCpf: string | null;
  dataInicioValidade: string | null;
  dataFimValidade: string | null;
  dataVencimentoUtil: string | null;
  nomeArquivo: string | null;
}

type CertificadoRow = {
  odooId: number;
  tipo: string | null;
  numeroSerie: string | null;
  proprietario: string | null;
  cnpjCpf: string | null;
  dataInicioValidade: Date | null;
  dataFimValidade: Date | null;
  dataVencimentoUtil: Date | null;
  nomeArquivo: string | null;
};

function toCertificadoLinha(r: CertificadoRow): CertificadoLinha {
  return {
    odooId: r.odooId,
    tipo: r.tipo,
    numeroSerie: r.numeroSerie,
    proprietario: r.proprietario,
    cnpjCpf: r.cnpjCpf,
    dataInicioValidade: dia(r.dataInicioValidade),
    dataFimValidade: dia(r.dataFimValidade),
    dataVencimentoUtil: dia(r.dataVencimentoUtil),
    nomeArquivo: r.nomeArquivo,
  };
}

/** Lista os certificados digitais cadastrados, do que vence primeiro para o
 * que vence por último. Volume baixo (~11): sem paginação. */
export async function queryCertificados(
  prisma: PrismaClient,
): Promise<{ linhas: CertificadoLinha[]; total: number }> {
  const [rows, total] = await Promise.all([
    prisma.fatoCertificado.findMany({ orderBy: { dataFimValidade: "asc" } }),
    prisma.fatoCertificado.count(),
  ]);
  return { linhas: rows.map(toCertificadoLinha), total };
}
```

- [ ] **Step 4: Rodar o teste e confirmar que passa**

Run: `npx jest src/lib/reports/queries/fiscal-complementar.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/lib/reports/queries/fiscal-complementar.ts src/lib/reports/queries/fiscal-complementar.test.ts
git commit -m "feat(f4-l1c): queryCertificados"
```

---

## Task 7: Tool `fiscal_certificados`

**Files:**
- Create: `mcp/tools/fiscal/certificados.ts`
- Modify: `mcp/tools/fiscal/index.ts`

- [ ] **Step 1: Criar a tool**

Criar `mcp/tools/fiscal/certificados.ts` (espelha `apuracao-fiscal.ts`):

```typescript
// mcp/tools/fiscal/certificados.ts
// Tool MCP: fiscal_certificados
import { z } from "zod";
import type { ToolEntry } from "../../catalog/types.js";
import { queryCertificados } from "@/lib/reports/queries/fiscal-complementar.js";
import { withFreshness } from "../../lib/freshness.js";

const inputSchema = z.object({});

const linha = z.object({
  odooId: z.number().int(),
  tipo: z.string().nullable(),
  numeroSerie: z.string().nullable(),
  proprietario: z.string().nullable(),
  cnpjCpf: z.string().nullable(),
  dataInicioValidade: z.string().nullable(),
  dataFimValidade: z.string().nullable(),
  dataVencimentoUtil: z.string().nullable(),
  nomeArquivo: z.string().nullable(),
});

const dados = z.object({
  linhas: z.array(linha),
  total: z.number().int(),
});

const fonteStatus = z.object({
  status: z.string(),
  ultimaSyncEm: z.string().nullable(),
});

const outputSchema = z.union([
  z.object({ estado: z.literal("preparando") }),
  z.object({
    estado: z.enum(["ok", "vazio"]),
    dados,
    atualizadoEm: z.string(),
    fonteStatus,
  }),
]);

type Input = z.infer<typeof inputSchema>;
type Output = z.infer<typeof outputSchema>;

export const fiscalCertificados: ToolEntry<Input, Output> = {
  id: "fiscal_certificados",
  dominio: "fiscal",
  descricao:
    "Certificados digitais (e-CNPJ) das empresas do grupo: tipo, número de " +
    "série, proprietário, CNPJ e datas de validade. Ordenado do que vence " +
    "primeiro para o último — útil para 'quais certificados estão perto de " +
    "vencer'. Não expõe a senha nem o arquivo do certificado.",
  inputSchemaShape: inputSchema.shape,
  inputSchema,
  outputSchema,
  handler: (_input, ctx) =>
    withFreshness(ctx.prisma, ["fato_certificado"], () =>
      queryCertificados(ctx.prisma),
    ),
};
```

- [ ] **Step 2: Registrar no índice do domínio fiscal**

Em `mcp/tools/fiscal/index.ts`, adicionar o import e a entrada no array `fiscalTools`:

```typescript
import { fiscalCertificados } from "./certificados.js";
```

```typescript
  fiscalCertificados as ToolEntry,
```

- [ ] **Step 3: Verificar typecheck do MCP**

Run: `npx tsc --noEmit -p mcp/tsconfig.json`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add mcp/tools/fiscal/certificados.ts mcp/tools/fiscal/index.ts
git commit -m "feat(f4-l1c): tool fiscal_certificados"
```

---

## Task 8: Catálogo — contagem, IDs e snapshot

**Files:**
- Modify: `mcp/__tests__/integration.test.ts`
- Modify: `src/lib/mcp-catalog-snapshot.json` (regenerado)

- [ ] **Step 1: Atualizar `FISCAL_IDS` e as contagens no teste de integração**

Em `mcp/__tests__/integration.test.ts`:
- No array `FISCAL_IDS`, adicionar `"fiscal_certificados"`.
- Trocar as asserções de contagem do catálogo de leitura de **45 para 46** e o catálogo bruto de **46 para 47**. Os pontos exatos (descrições dos `it` e `expect(...).toHaveLength(...)`): `super_admin recebe EXATAMENTE 45 tools` → 46; `catálogo bruto ... 46 entradas` → 47; `super_admin vê todas as 45 tools` (2 asserções) → 46; `admin vê todas as 45 tools` → 46; `tools/list via HTTP retorna 45 tools` → 46. (Mesmo conjunto de pontos editado no commit `088cb91`.)
- Se houver um `it` que conte as tools do domínio fiscal por um número literal na descrição (ex.: "N tools de fiscal"), incrementar o número. As asserções que iteram `for (const id of FISCAL_IDS)` se ajustam sozinhas. O passo seguinte (rodar o teste) revela qualquer contagem literal restante.

- [ ] **Step 2: Rodar o teste de integração**

Run: `npx jest mcp/__tests__/integration.test.ts`
Expected: PASS — 46 tools de leitura, catálogo bruto 47.

- [ ] **Step 3: Regenerar o snapshot do catálogo**

Run: `npm run gen:mcp-catalog`
Expected: imprime `Catálogo MCP serializado: 47 tools`; `src/lib/mcp-catalog-snapshot.json` atualizado com `fiscal_certificados`.

- [ ] **Step 4: Verificar o teste do snapshot**

Run: `npx jest src/lib/actions/mcp-catalog-schema.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add mcp/__tests__/integration.test.ts src/lib/mcp-catalog-snapshot.json
git commit -m "feat(f4-l1c): registra fiscal_certificados no catalogo (46 tools) + snapshot"
```

---

## Task 9: `bi-schema-reference.ts` — `fato_certificado`

**Files:**
- Modify: `src/lib/agent/bi-schema-reference.ts`

- [ ] **Step 1: Adicionar o bloco `TABLE fato_certificado`**

Em `src/lib/agent/bi-schema-reference.ts`, logo após o bloco `TABLE fato_carta_correcao (...)`:

```sql
-- Certificados digitais (e-CNPJ) das empresas
TABLE fato_certificado (
  odoo_id              INT PRIMARY KEY,
  tipo                 TEXT,          -- ex.: A1
  numero_serie         TEXT,
  proprietario         TEXT,
  cnpj_cpf             TEXT,
  data_inicio_validade TIMESTAMPTZ,
  data_fim_validade    TIMESTAMPTZ,
  data_vencimento_util TIMESTAMPTZ,
  nome_arquivo         TEXT,
  atualizado_em        TIMESTAMPTZ
);
```

(`raw_finan_baixa_lancamento` e `raw_pedido_faturamento` **não** entram aqui — os roles do MCP não leem `raw_*`; ver spec §2.2.)

- [ ] **Step 2: Verificar o teste-trava do bi-schema**

Run: `npx jest src/lib/agent/bi-schema-reference.test.ts`
Expected: PASS. Se o teste exigir atualização explícita (lista de tabelas esperadas), incluir `fato_certificado` lá.

- [ ] **Step 3: Commit**

```bash
git add src/lib/agent/bi-schema-reference.ts
git commit -m "feat(f4-l1c): fato_certificado no bi-schema-reference (Caminho 3c)"
```

---

## Task 10: Verde final e verificação

**Files:** nenhum (só verificação).

- [ ] **Step 1: Bateria de verde completa**

Run, um a um, confirmando sucesso:
- `npx tsc --noEmit`
- `npx tsc --noEmit -p mcp/tsconfig.json`
- `npx eslint mcp/tools/fiscal src/worker/fatos src/worker/odoo src/lib/reports/queries src/lib/agent`
- `npx jest`
- `npx next build`

Expected: tudo PASS. `jest` com os testes novos (field-selection, fato-certificado, queryCertificados, integration) verdes.

- [ ] **Step 2: Build do container MCP**

Run: `docker compose build mcp`
Expected: build conclui sem erro.

- [ ] **Step 3: Commit (se algum verde exigiu ajuste)**

```bash
git add -A
git commit -m "chore(f4-l1c): ajustes da bateria de verde"
```

(Se nada mudou, pular este passo.)

---

## Task 11: Ingestão e smoke test (parte da Onda I)

> Esta task pertence à **Onda I** do plano-mãe (`TI.1`–`TI.7`), executada depois de L1b e L1c. Listada aqui para o smoke test específico da L1c.

**Files:**
- Create: `scripts/f4l-smoke-l1c.ts` (smoke descartável; pode ser removido após).

- [ ] **Step 1: Subir o stack e aplicar migrations**

Run:
- `docker compose up -d db redis`
- `npx prisma migrate deploy`
- `npm run db:provision` (reaplica os GRANT; `fato_certificado` é coberto pelo loop dinâmico).

- [ ] **Step 2: Rodar o sync dos 3 modelos novos**

Run: `tsx --env-file=.env.local scripts/f4l-ingest.ts` (ciclo completo) **ou** um sync direcionado dos 3 modelos. Depois `tsx --env-file=.env.local scripts/f4l-build-fatos.ts` para construir `fato_certificado`.

- [ ] **Step 3: Conferir contagem contra o Odoo**

Para cada modelo novo, comparar `count(raw_*)` no Postgres ao `search_count` do Odoo:
- `raw_sped_certificado` ≈ 11
- `raw_finan_baixa_lancamento` ≈ 3
- `raw_pedido_faturamento` ≈ 1 (pode crescer)

Confirmar também, via `psql`, que `raw_sped_certificado` linha-a-linha **não tem** as chaves `senha` nem `arquivo` no JSONB `data` (`SELECT data ? 'senha', data ? 'arquivo' FROM raw_sped_certificado;` → ambos `false`).

- [ ] **Step 4: Smoke test da tool**

Escrever `scripts/f4l-smoke-l1c.ts` que importa `queryCertificados` e a roda contra o cache, imprimindo as linhas. Confirmar que os 11 certificados aparecem com validade, sem `senha`/`arquivo`.

Run: `tsx --env-file=.env.local scripts/f4l-smoke-l1c.ts`
Expected: lista de certificados com datas de validade plausíveis.

- [ ] **Step 5: Commit do resultado**

```bash
git add scripts/f4l-smoke-l1c.ts
git commit -m "test(f4-l1c): smoke test da ingestao do residuo 4a"
```

---

## Critérios de pronto (da spec §4)

1. Os 3 modelos têm `Raw*`, entrada em `MODEL_CATALOG`, migration `f4l_residuo_4a` aplicada.
2. `FatoCertificado` + builder + teste unitário verde.
3. `fiscal_certificados` visível para domínio `fiscal`, ausente para quem não tem, responde com dado real.
4. `fato_certificado` no `bi-schema-reference.ts`, coberto pelo GRANT dinâmico.
5. `raw_sped_certificado` sem `senha`/`arquivo`.
6. Verde: `tsc` (raiz + mcp), `eslint`, `jest`, `next build`, `docker compose build mcp`.
7. Snapshot do catálogo regenerado (47 entradas).
8. Contagem conferida contra o Odoo na Onda I.
