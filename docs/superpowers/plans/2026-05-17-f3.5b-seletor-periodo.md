# F3.5b — Seletor de período v1 — Plano de implementação

> **Para workers:** executar task a task, microtarefas, TDD onde houver lógica.
> Spec-fonte: `docs/superpowers/specs/2026-05-17-f3.5b-seletor-periodo-design.md` (v3).
> Steps marcados com checkbox.

**Goal:** Substituir o `PeriodFilter` cru por uma `PeriodBar` moderna (pílulas
+ calendário de meses) nos dois relatórios temporais, com estado 100% na URL.

**Architecture:** Camada pura `periodo.ts` resolve searchParams → período;
`page.tsx` injeta o período nos `ReportFilterValues`; `PeriodBar` +
`CustomRangePopover` escrevem a URL via `router.push`.

**Tech Stack:** Next.js 16 App Router, TypeScript, Tailwind v4, base-ui,
lucide-react, framer-motion, Jest + Testing Library.

**Branch:** `feat/ingestao` (atual).

---

## Estrutura de arquivos

| Arquivo | Responsabilidade | Ação |
|---|---|---|
| `src/lib/reports/periodo.ts` | lógica pura de período | criar |
| `src/lib/reports/periodo.test.ts` | testes da lógica pura | criar |
| `src/lib/reports/types.ts` | `temporal` em `ReportEntry`; tira `"periodo"` | modificar |
| `src/lib/reports/catalog.ts` | migra os 2 relatórios temporais | modificar |
| `src/lib/reports/filters.ts` | tira caso `periodo` + helpers órfãos | modificar |
| `src/components/reports/report-filters.tsx` | tira caso `periodo` | modificar |
| `src/components/reports/filter-controls/period-filter.tsx` | — | remover |
| `src/components/ui/popover.tsx` | wrapper base-ui Popover | criar |
| `src/components/reports/custom-range-popover.tsx` | seletor de intervalo de meses | criar |
| `src/components/reports/custom-range-popover.test.tsx` | testes | criar |
| `src/components/reports/period-bar.tsx` | barra de pílulas de período | criar |
| `src/components/reports/period-bar.test.tsx` | testes | criar |
| `src/app/(protected)/relatorios/[id]/page.tsx` | resolve + injeta período | modificar |
| `src/app/(protected)/relatorios/[id]/report-view.tsx` | renderiza `PeriodBar` | modificar |

---

## Task 1: Camada pura `periodo.ts` (TDD)

**Files:**
- Create: `src/lib/reports/periodo.ts`
- Test: `src/lib/reports/periodo.test.ts`

- [ ] **Step 1: Escrever `periodo.test.ts` (falhando)**

```ts
import {
  ehMesValido, mesCorrente, resolverPeriodo, periodoParaParams, rotuloPeriodo,
} from "./periodo";

const HOJE = new Date(Date.UTC(2026, 4, 17)); // 2026-05

describe("ehMesValido", () => {
  it("aceita YYYY-MM", () => expect(ehMesValido("2026-05")).toBe(true));
  it("rejeita formatos errados", () => {
    for (const s of ["2026-5", "2026/05", "abc", "2026-13", "", "2026-00"]) {
      expect(ehMesValido(s)).toBe(false);
    }
  });
});

describe("mesCorrente", () => {
  it("retorna o mês de hoje em UTC", () => {
    expect(mesCorrente(HOJE)).toBe("2026-05");
  });
});

describe("resolverPeriodo", () => {
  it("preset mes", () => {
    expect(resolverPeriodo({ periodo: "mes" }, "3meses", HOJE))
      .toEqual({ preset: "mes", de: "2026-05", ate: "2026-05" });
  });
  it("preset 3meses = 3 meses inclusivos", () => {
    expect(resolverPeriodo({ periodo: "3meses" }, "3meses", HOJE))
      .toEqual({ preset: "3meses", de: "2026-03", ate: "2026-05" });
  });
  it("preset ano", () => {
    expect(resolverPeriodo({ periodo: "ano" }, "3meses", HOJE))
      .toEqual({ preset: "ano", de: "2026-01", ate: "2026-05" });
  });
  it("preset tudo zera de/ate", () => {
    expect(resolverPeriodo({ periodo: "tudo" }, "3meses", HOJE))
      .toEqual({ preset: "tudo", de: null, ate: null });
  });
  it("sem periodo usa o padrao", () => {
    expect(resolverPeriodo({}, "ano", HOJE).preset).toBe("ano");
  });
  it("periodo desconhecido cai no padrao", () => {
    expect(resolverPeriodo({ periodo: "xpto" }, "mes", HOJE).preset).toBe("mes");
  });
  it("custom valido", () => {
    expect(resolverPeriodo({ periodo: "custom", de: "2026-01", ate: "2026-03" }, "mes", HOJE))
      .toEqual({ preset: "custom", de: "2026-01", ate: "2026-03" });
  });
  it("custom com de > ate troca as pontas", () => {
    expect(resolverPeriodo({ periodo: "custom", de: "2026-03", ate: "2026-01" }, "mes", HOJE))
      .toEqual({ preset: "custom", de: "2026-01", ate: "2026-03" });
  });
  it("custom com so uma ponta valida cai no padrao", () => {
    expect(resolverPeriodo({ periodo: "custom", de: "2026-01" }, "mes", HOJE).preset).toBe("mes");
    expect(resolverPeriodo({ periodo: "custom", de: "x", ate: "2026-03" }, "mes", HOJE).preset).toBe("mes");
  });
  it("custom recorta mes futuro ao mes corrente", () => {
    expect(resolverPeriodo({ periodo: "custom", de: "2026-01", ate: "2026-12" }, "mes", HOJE))
      .toEqual({ preset: "custom", de: "2026-01", ate: "2026-05" });
  });
});

describe("periodoParaParams", () => {
  it("emite periodo sem de/ate em preset nao-custom", () => {
    expect(periodoParaParams({ preset: "3meses", de: "2026-03", ate: "2026-05" }))
      .toEqual({ periodo: "3meses" });
  });
  it("emite de/ate em custom", () => {
    expect(periodoParaParams({ preset: "custom", de: "2026-01", ate: "2026-03" }))
      .toEqual({ periodo: "custom", de: "2026-01", ate: "2026-03" });
  });
  it("ida e volta com resolverPeriodo", () => {
    const p = resolverPeriodo({ periodo: "custom", de: "2026-01", ate: "2026-03" }, "mes", HOJE);
    expect(resolverPeriodo(periodoParaParams(p), "mes", HOJE)).toEqual(p);
  });
});

describe("rotuloPeriodo", () => {
  it("tudo", () => expect(rotuloPeriodo({ preset: "tudo", de: null, ate: null })).toBe("Tudo"));
  it("mes unico", () => expect(rotuloPeriodo({ preset: "mes", de: "2026-03", ate: "2026-03" })).toBe("mar/2026"));
  it("intervalo", () => expect(rotuloPeriodo({ preset: "custom", de: "2026-01", ate: "2026-03" })).toBe("jan/2026 – mar/2026"));
});
```

- [ ] **Step 2: Rodar e ver falhar** — `npx jest src/lib/reports/periodo.test.ts` → FAIL (módulo não existe).

- [ ] **Step 3: Implementar `periodo.ts`**

```ts
// src/lib/reports/periodo.ts

/** Presets escolhíveis na barra de período. */
export type PeriodoPreset = "mes" | "3meses" | "ano" | "tudo" | "custom";

/** Subconjunto válido como padrão de catálogo — "custom" exige de/ate. */
export type PeriodoPresetPadrao = Exclude<PeriodoPreset, "custom">;

/** Par de meses inclusivo; de/ate null = sem limite (preset "tudo"). */
export interface PeriodoResolvido {
  preset: PeriodoPreset;
  de: string | null;
  ate: string | null;
}

const MES_REGEX = /^\d{4}-(0[1-9]|1[0-2])$/;
const MESES_ABREV = [
  "jan", "fev", "mar", "abr", "mai", "jun",
  "jul", "ago", "set", "out", "nov", "dez",
];

/** Valida o formato YYYY-MM (mês 01–12). */
export function ehMesValido(s: string): boolean {
  return typeof s === "string" && MES_REGEX.test(s);
}

/** Converte "YYYY-MM" num índice absoluto de mês (ano*12 + mês0). */
function mesParaIndice(m: string): number {
  const [ano, mes] = m.split("-").map(Number);
  return ano * 12 + (mes - 1);
}

/** Converte um índice absoluto de mês de volta para "YYYY-MM". */
function indiceParaMes(i: number): string {
  const ano = Math.floor(i / 12);
  const mes = (i % 12) + 1;
  return `${ano}-${String(mes).padStart(2, "0")}`;
}

/** Mês corrente no formato "YYYY-MM", em UTC. */
export function mesCorrente(hoje: Date = new Date()): string {
  return `${hoje.getUTCFullYear()}-${String(hoje.getUTCMonth() + 1).padStart(2, "0")}`;
}

/**
 * Resolve searchParams + padrão do catálogo num período concreto.
 * Aplica todas as regras da spec §"Regras de resolverPeriodo".
 */
export function resolverPeriodo(
  params: { periodo?: string; de?: string; ate?: string },
  padrao: PeriodoPresetPadrao,
  hoje: Date = new Date(),
): PeriodoResolvido {
  const corrente = mesCorrente(hoje);
  const idxCorrente = mesParaIndice(corrente);

  const resolverPreset = (preset: PeriodoPresetPadrao): PeriodoResolvido => {
    if (preset === "mes") return { preset, de: corrente, ate: corrente };
    if (preset === "3meses") {
      return { preset, de: indiceParaMes(idxCorrente - 2), ate: corrente };
    }
    if (preset === "ano") {
      return { preset, de: `${hoje.getUTCFullYear()}-01`, ate: corrente };
    }
    return { preset: "tudo", de: null, ate: null };
  };

  const p = params.periodo;
  if (p === "custom") {
    const { de, ate } = params;
    if (!de || !ate || !ehMesValido(de) || !ehMesValido(ate)) {
      return resolverPreset(padrao);
    }
    let di = mesParaIndice(de);
    let ai = mesParaIndice(ate);
    if (di > ai) [di, ai] = [ai, di];
    if (di > idxCorrente) di = idxCorrente;
    if (ai > idxCorrente) ai = idxCorrente;
    return { preset: "custom", de: indiceParaMes(di), ate: indiceParaMes(ai) };
  }
  if (p === "mes" || p === "3meses" || p === "ano" || p === "tudo") {
    return resolverPreset(p);
  }
  return resolverPreset(padrao);
}

/**
 * Serializa um período num objeto de searchParams. Sempre inclui `periodo`;
 * inclui `de`/`ate` apenas quando `preset === "custom"`.
 */
export function periodoParaParams(p: PeriodoResolvido): Record<string, string> {
  if (p.preset === "custom" && p.de && p.ate) {
    return { periodo: "custom", de: p.de, ate: p.ate };
  }
  return { periodo: p.preset };
}

/** Formata "YYYY-MM" como "mmm/aaaa" (ex.: "mar/2026"). */
function formatarMes(m: string): string {
  const [ano, mes] = m.split("-").map(Number);
  return `${MESES_ABREV[mes - 1]}/${ano}`;
}

/**
 * Rótulo legível de um período: "Tudo" | "mar/2026" | "jan/2026 – mar/2026".
 * Consumido apenas pela pílula "Personalizado" da PeriodBar.
 */
export function rotuloPeriodo(p: PeriodoResolvido): string {
  if (!p.de || !p.ate) return "Tudo";
  if (p.de === p.ate) return formatarMes(p.de);
  return `${formatarMes(p.de)} – ${formatarMes(p.ate)}`;
}
```

- [ ] **Step 4: Rodar e ver passar** — `npx jest src/lib/reports/periodo.test.ts` → PASS.

- [ ] **Step 5: Commit** — `git add` ambos + `git commit -m "feat(reports): camada pura de período (periodo.ts)"`.

---

## Task 2: `types.ts` — campo `temporal` e remoção de `"periodo"`

**Files:** Modify: `src/lib/reports/types.ts`

- [ ] **Step 1:** Em `ReportFilterTipo`, remover o membro `"periodo"` da união.
- [ ] **Step 2:** Adicionar `import type { PeriodoPresetPadrao } from "./periodo";` no topo.
- [ ] **Step 3:** Em `ReportEntry`, adicionar o campo opcional, após `secoes`:

```ts
  /** Declara que o relatório tem dimensão temporal. Ausente = snapshot. */
  temporal?: {
    periodoPadrao: PeriodoPresetPadrao;
  };
```

- [ ] **Step 4:** Rodar `npx tsc --noEmit` → vai falhar em `catalog.ts`,
  `filters.ts`, `report-filters.tsx` (uso de `"periodo"`). Esperado — corrigido
  nas tasks 3–5. **Não commitar ainda** — commit conjunto no fim da Task 5.

---

## Task 3: `catalog.ts` — migrar os 2 relatórios temporais

**Files:** Modify: `src/lib/reports/catalog.ts`

- [ ] **Step 1:** No relatório `entradas-saidas`: adicionar
  `temporal: { periodoPadrao: "3meses" },` no objeto do relatório (irmão de
  `secoes`), e na seção `linha` trocar
  `filtros: [{ tipo: "periodo", default: "3" }, { tipo: "armazem" }]` por
  `filtros: [{ tipo: "armazem" }]`.
- [ ] **Step 2:** No relatório `top-movimentados`: adicionar
  `temporal: { periodoPadrao: "3meses" },` no objeto do relatório, e na seção
  `barras` trocar
  `filtros: [{ tipo: "periodo", default: "3" }, { tipo: "sentido" }]` por
  `filtros: [{ tipo: "sentido" }]`.
- [ ] **Step 3:** `npx tsc --noEmit` — `catalog.ts` deixa de acusar erro.

---

## Task 4: `filters.ts` — remover caso `periodo` + helpers órfãos

**Files:** Modify: `src/lib/reports/filters.ts`

- [ ] **Step 1:** Remover o bloco `if (tipos.has("periodo")) { … }` inteiro.
- [ ] **Step 2:** Remover as funções `mesAtual` e `mesAtras` e a constante
  `MES_REGEX` (ficam órfãs).
- [ ] **Step 3:** Confirmar: `grep -rn "mesAtual\|mesAtras\|MES_REGEX" src/lib/reports/filters.ts`
  → sem resultado. `grep -rn "periodoDe\|periodoAte" src/lib/reports/filters.ts`
  → sem resultado.
- [ ] **Step 4:** `npx jest src/lib/reports/filters.test.ts` → se houver caso
  testando `periodo`, removê-lo (o período não é mais filtro de seção).

---

## Task 5: `report-filters.tsx` — remover caso `periodo` + remover `PeriodFilter`

**Files:**
- Modify: `src/components/reports/report-filters.tsx`
- Remove: `src/components/reports/filter-controls/period-filter.tsx` (+ teste se houver)

- [ ] **Step 1:** Em `report-filters.tsx`, remover o `case "periodo":` inteiro
  do `switch` e o `import { PeriodFilter }`.
- [ ] **Step 2:** `git rm src/components/reports/filter-controls/period-filter.tsx`
  (e o `.test.tsx` correspondente se existir — verificar com `ls`).
- [ ] **Step 3:** `npx tsc --noEmit` → limpo. `npx jest` → verde.
- [ ] **Step 4: Commit** das tasks 2–5 juntas —
  `git commit -m "refactor(reports): período deixa de ser filtro de seção e vira dimensão (campo temporal)"`.

---

## Task 6: `ui/popover.tsx` — wrapper base-ui Popover

**Files:** Create: `src/components/ui/popover.tsx`

- [ ] **Step 1:** Criar o wrapper, espelhando o padrão de `select.tsx`
  (`data-slot`, `cn`, classes do design system, animações `data-open`/`data-closed`):

```tsx
"use client";

import { Popover as PopoverPrimitive } from "@base-ui/react/popover";
import { cn } from "@/lib/utils";

const Popover = PopoverPrimitive.Root;
const PopoverTrigger = PopoverPrimitive.Trigger;

function PopoverContent({
  className,
  side = "bottom",
  sideOffset = 6,
  align = "start",
  ...props
}: PopoverPrimitive.Popup.Props &
  Pick<PopoverPrimitive.Positioner.Props, "side" | "sideOffset" | "align">) {
  return (
    <PopoverPrimitive.Portal>
      <PopoverPrimitive.Positioner
        side={side}
        sideOffset={sideOffset}
        align={align}
        className="isolate z-50"
      >
        <PopoverPrimitive.Popup
          data-slot="popover-content"
          className={cn(
            "z-50 w-72 origin-(--transform-origin) rounded-xl bg-popover p-4 text-popover-foreground shadow-lg ring-1 ring-foreground/10 outline-none",
            "data-open:animate-in data-open:fade-in-0 data-open:zoom-in-95",
            "data-closed:animate-out data-closed:fade-out-0 data-closed:zoom-out-95",
            className,
          )}
          {...props}
        />
      </PopoverPrimitive.Positioner>
    </PopoverPrimitive.Portal>
  );
}

export { Popover, PopoverTrigger, PopoverContent };
```

- [ ] **Step 2:** `npx tsc --noEmit` → limpo. Confirmar os nomes das parts
  contra `node_modules/@base-ui/react/popover/index.parts.*` (Root, Trigger,
  Portal, Positioner, Popup). Ajustar se a API divergir.
- [ ] **Step 3: Commit** — `git commit -m "feat(ui): wrapper Popover do design system"`.

---

## Task 7: `CustomRangePopover` (TDD)

**Files:**
- Create: `src/components/reports/custom-range-popover.tsx`
- Test: `src/components/reports/custom-range-popover.test.tsx`

Consultar a skill `ui-ux-pro-max` antes de codar (componente de UI).

**Contrato do componente:**

```ts
interface CustomRangePopoverProps {
  /** Período corrente — pré-seleciona os meses ao abrir. */
  periodo: PeriodoResolvido;
  /** Chamado ao aplicar um intervalo válido. */
  onAplicar: (de: string, ate: string) => void;
  /** Elemento gatilho (a pílula "Personalizado" da PeriodBar). */
  children: React.ReactNode;
}
```

**Comportamento:**
- Usa `Popover`/`PopoverTrigger`/`PopoverContent` (Task 6); `children` é o
  conteúdo do `PopoverTrigger` (`render` prop ou child).
- Estado interno: `de: string | null`, `ate: string | null`, `anoVisivel:
  number`. Inicializa `de`/`ate` de `periodo` (se `periodo.de`/`ate`); `ano
  visível` = ano de `periodo.ate` ou ano de `mesCorrente()`.
- Grade de 12 meses do `anoVisivel`; chevrons `‹ ›` trocam o ano.
- "Mês corrente" para desabilitar meses futuros vem de `mesCorrente()` de
  `periodo.ts`. Meses futuros: `disabled`, `aria-disabled`, opacidade reduzida.
- Clique num mês: 1º clique define `de` e zera `ate`; 2º clique define `ate`
  (se anterior a `de`, troca). Mês selecionado: `bg-primary text-primary-foreground`;
  meses no intervalo: `bg-primary/15`.
- Rodapé: `Cancelar` (fecha sem aplicar) e `Aplicar` (`Button`); `Aplicar`
  `disabled` enquanto `de`/`ate` não estiverem ambos preenchidos.
- `Aplicar`: chama `onAplicar(de, ate)` e fecha o popover.
- Cada botão de mês ≥ 44px de alvo; `cursor-pointer`; foco visível.

- [ ] **Step 1: Escrever `custom-range-popover.test.tsx` (falhando)** — casos:
  - renderiza o gatilho; clicar abre o popover (12 botões de mês visíveis);
  - "Aplicar" começa `disabled`;
  - selecionar dois meses habilita "Aplicar"; clicar em "Aplicar" chama
    `onAplicar` com os dois meses em ordem crescente;
  - um mês futuro (relativo a um `mesCorrente` mockado — usar um período cujo
    `ate` force o ano corrente) renderiza `disabled`.

  Mock de `periodo.ts` não é necessário (lógica pura, real). Para fixar o mês
  corrente no teste, se preciso, usar `jest.useFakeTimers().setSystemTime(...)`.

  **Nota jsdom:** componentes interativos do base-ui exigiram um polyfill de
  `PointerEvent` no ambiente de teste na F3 (ver `access-step` tests). Se o
  popover não abrir no clique, verificar se já há polyfill global em
  `jest.setup`; se não, adicionar o mesmo polyfill localmente no topo deste
  arquivo de teste.

- [ ] **Step 2:** Rodar → FAIL.
- [ ] **Step 3:** Implementar `custom-range-popover.tsx` conforme o contrato.
- [ ] **Step 4:** Rodar → PASS. `npx tsc --noEmit` limpo.
- [ ] **Step 5: Commit** — `git commit -m "feat(reports): CustomRangePopover (seletor de intervalo de meses)"`.

---

## Task 8: `PeriodBar` (TDD)

**Files:**
- Create: `src/components/reports/period-bar.tsx`
- Test: `src/components/reports/period-bar.test.tsx`

Consultar a skill `ui-ux-pro-max` antes de codar.

**Contrato:**

```ts
interface PeriodBarProps {
  periodo: PeriodoResolvido;
}
```

**Comportamento:**
- Client component. Lê `useRouter`, `usePathname`, `useSearchParams`.
- `aplicar(p: PeriodoResolvido)`: clona `searchParams`, **deleta** `periodo`,
  `de`, `ate`, aplica `periodoParaParams(p)` (que reescreve `periodo` e, só em
  custom, `de`/`ate`), faz `router.push(\`${pathname}?${params}\`)`. Preserva
  todos os demais params (busca, `armazemId`, `sentido`). A `PeriodBar` **não**
  chama `resolverPeriodo` — ela só serializa via `periodoParaParams`.
- Rótulo "PERÍODO" (`text-xs uppercase tracking-wide text-muted-foreground`).
- Container `role="radiogroup" aria-label="Período"`.
- 4 pílulas de preset (`mes`, `3meses`, `ano`, `tudo`) + 1 pílula
  "Personalizado". Cada pílula: `role="radio"`, `aria-checked` = (`preset ===
  periodo.preset`), `tabIndex={0}` (navegação por `Tab`; **roving tabindex /
  setas ficam fora do escopo v1** — `Tab` + `radiogroup`/`radio` já dão
  semântica correta de leitor de tela).
- Pílula ativa: `bg-primary text-primary-foreground`; inativa:
  `bg-muted hover:bg-muted/70`. Transição 150–200ms. Alvo ≥ 44px,
  `cursor-pointer`.
- Clique numa pílula de preset → `aplicar({ preset, de: null, ate: null })`.
  `periodoParaParams` desse valor devolve só `{ periodo: preset }`, e o
  `delete` prévio de `de`/`ate` garante que trocar de `custom` para um preset
  limpa os params antigos.
- Pílula "Personalizado": envolta em `CustomRangePopover` com
  `periodo={periodo}` e `onAplicar={(de, ate) => aplicar({ preset: "custom",
  de, ate })}`. Quando `periodo.preset === "custom"`, o texto da pílula é
  `rotuloPeriodo(periodo)`; senão, "Personalizado". Ícone `CalendarDays`
  (lucide). A pílula custom também é um `role="radio"` com `aria-checked`.

- [ ] **Step 1: Escrever `period-bar.test.tsx` (falhando)** — mock de
  `next/navigation` no padrão de `report-view.test.tsx`, capturando `push`:

```tsx
const push = jest.fn();
jest.mock("next/navigation", () => ({
  useRouter: () => ({ push }),
  usePathname: () => "/relatorios/entradas-saidas",
  useSearchParams: () => new URLSearchParams("armazemId=7"),
}));
```

  Casos:
  - renderiza 5 pílulas (`getAllByRole("radio")` → 5);
  - container tem `role="radiogroup"`;
  - com `periodo={preset:"3meses",…}`, a pílula "Últimos 3 meses" tem
    `aria-checked="true"` e as demais `"false"`;
  - clicar na pílula "Este mês" chama `push` com uma URL que contém
    `periodo=mes` **e preserva** `armazemId=7`.

- [ ] **Step 2:** Rodar → FAIL.
- [ ] **Step 3:** Implementar `period-bar.tsx` conforme o contrato.
- [ ] **Step 4:** Rodar → PASS. `npx tsc --noEmit` limpo.
- [ ] **Step 5: Commit** — `git commit -m "feat(reports): PeriodBar (pílulas de período + estado na URL)"`.

---

## Task 9: Integração `page.tsx` + `report-view.tsx`

**Files:**
- Modify: `src/app/(protected)/relatorios/[id]/page.tsx`
- Modify: `src/app/(protected)/relatorios/[id]/report-view.tsx`

- [ ] **Step 1: `page.tsx`** — após `const report = getReport(id);` e o
  `notFound`/`requireDomainAccess`, resolver o período:

```ts
import { resolverPeriodo, type PeriodoResolvido } from "@/lib/reports/periodo";
// …
const periodo: PeriodoResolvido | null = report.temporal
  ? resolverPeriodo(sp, report.temporal.periodoPadrao)
  : null;
```

- [ ] **Step 2: `page.tsx`** — no laço sobre `report.secoes`, injetar o
  período nos filtros antes da query:

```ts
const filtros = parseFilters(secao, sp);
if (periodo) {
  filtros.periodoDe = periodo.de ?? undefined;
  filtros.periodoAte = periodo.ate ?? undefined;
}
const resultado = await query(filtros);
```

- [ ] **Step 3: `page.tsx`** — passar `periodo` ao `ReportView`:
  `<ReportView report={report} secoes={secoes} freshness={freshness} options={options} periodo={periodo} />`.

- [ ] **Step 4: `report-view.tsx`** — em `ReportViewProps`, adicionar
  `periodo: PeriodoResolvido | null;` (importar o tipo). Importar `PeriodBar`.
  No JSX, antes do `<ReportFilters …>`, renderizar a barra quando houver
  período:

```tsx
{periodo ? <PeriodBar periodo={periodo} /> : null}
```

- [ ] **Step 5:** `npx tsc --noEmit` → limpo. `npx jest "src/app/(protected)/relatorios"`
  → ajustar `report-view.test.tsx` se necessário (passar `periodo={null}` no
  render do teste existente, que usa um relatório de snapshot).
- [ ] **Step 6: Commit** — `git commit -m "feat(reports): integra PeriodBar nos relatórios temporais"`.

---

## Task 10: Verificação final

- [ ] **Step 1:** `npx tsc --noEmit` → limpo.
- [ ] **Step 2:** `npx eslint src/lib/reports src/components/reports src/components/ui/popover.tsx "src/app/(protected)/relatorios"` → limpo.
- [ ] **Step 3:** `npx jest` → toda a suíte verde.
- [ ] **Step 4:** `npx next build` → sucesso.
- [ ] **Step 5:** Verificação funcional no dev server (porta 3000): abrir
  `/relatorios/entradas-saidas` e `/relatorios/top-movimentados` — a `PeriodBar`
  aparece; trocar de preset muda a URL e o gráfico; "Personalizado" abre o
  popover e aplica intervalo; recarregar mantém o estado; relatório de snapshot
  (`/relatorios/saldo-produto`) não mostra a barra.
- [ ] **Step 6:** Não há commit aqui — só verificação. Se algo falhar, corrigir
  na task correspondente.

---

## Cobertura da spec (self-review)

| Requisito da spec v3 | Task |
|---|---|
| `periodo.ts` (mesCorrente, ehMesValido, resolverPeriodo, periodoParaParams, rotuloPeriodo) | 1 |
| Regras de resolução (custom ambos válidos, de>ate, futuro, desconhecido) | 1 |
| `temporal` em `ReportEntry`; `ReportFilterTipo` sem `"periodo"` | 2 |
| Catálogo: 2 relatórios migrados | 3 |
| `parseFilters` sem `periodo` + helpers órfãos | 4 |
| `report-filters.tsx` sem `periodo`; `PeriodFilter` removido | 5 |
| `ui/popover.tsx` | 6 |
| `CustomRangePopover` | 7 |
| `PeriodBar` (radiogroup, preserva params) | 8 |
| Integração `page.tsx`/`report-view.tsx` | 9 |
| Testes + `tsc`/`lint`/`jest`/`build` | 1,7,8,10 |
| Critério de pronto | 10 |

---

## Histórico de revisões do plano

### Review #1 (PLAN v1 → v2) — lacunas, ordem, premissas

- **P1-B — over-engineering na `PeriodBar`.** A v1 mandava a `PeriodBar`
  chamar `resolverPeriodo` no client para montar o `PeriodoResolvido` de um
  preset. Desnecessário: para um preset basta gravar `periodo=<preset>` na URL.
  A v2 troca para `aplicar(p)` + `periodoParaParams`, sem `resolverPeriodo` no
  client.
- **P1-C — escopo de acessibilidade.** A v1 pedia roving tabindex + navegação
  por setas no radiogroup — código e testes não triviais, fora do escopo de um
  seletor v1. A v2 mantém `role="radiogroup"`/`radio`/`aria-checked` (semântica
  correta, atende R2-F da spec) mas com `tabIndex={0}` e navegação por `Tab`.
- **P1-A (mantido).** Tasks 2–5 compartilham um único commit porque a remoção
  de um membro de união de tipo não compila enquanto está parcial. É um refactor
  atômico; o plano deixa isso explícito e verifica `tsc` ao fim da Task 5.

### Review #2 (PLAN v2 → v3) — granularidade, integração, testabilidade

- **P2-A — `periodoParaParams` órfã.** Com P1-B, a `PeriodBar` quase não usava
  `periodoParaParams`, deixando-a coberta só por teste. A v3 fecha: a
  `PeriodBar` usa `periodoParaParams` **uniformemente** (preset e custom),
  construindo um `PeriodoResolvido` mínimo (`de/ate` null para presets) — DRY,
  e a função tem consumidor real.
- **P2-B — base-ui em jsdom.** O teste do `CustomRangePopover` pode precisar do
  polyfill de `PointerEvent` (precedente da F3, `access-step` tests). A v3
  adiciona a nota na Task 7 Step 1.
- **P2-C — `aplicar` deve deletar `de`/`ate`.** Ao trocar de `custom` para um
  preset, `periodoParaParams` não emite `de`/`ate`; sem deletá-los antes, eles
  vazariam na URL. A v3 explicita o `delete` de `periodo`/`de`/`ate` antes de
  aplicar os novos params na Task 8.
- Releitura final: tasks bem decompostas, ordem de dependências correta
  (1→2-5→6→7→8→9→10), sem placeholder, tipos consistentes entre tasks
  (`PeriodoResolvido`, `PeriodoPresetPadrao`, `periodoParaParams`,
  `mesCorrente`). **Plano liberado para execução.**
