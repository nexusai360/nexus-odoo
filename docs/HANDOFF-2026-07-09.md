# HANDOFF , troca de sessao 2026-07-09

> Ponto de retomada. Ler junto com STATUS.md.

## EM PRODUCAO (feito, deploy validado)
Merge grande na `main` (commit 448dc67e, PR #157) subiu para prod
(agentenex.nexusai360.com) TODO o trabalho local: **Diretoria** (Vendas/Pedidos/
Estoque/Visao-geral/Agenda) + **Nex reconstruido** + **Configuracoes** + **Relatorios
2.0 / Construtor F6** + correcoes (OOM do worker, venda futura da Mariane). Deploy
rolling OK, **103 migrations** aplicadas (base 95 + F6 7 + diretoria 1, idempotente),
/api/health OK, telas OK. Banco real intacto.

Branches preservadas no GitHub: feat/menu-diretoria, feat/prod-integracao,
feat/nex-reconstrucao, feat/config-acesso-menus (todas pushadas).

## FRENTE B , CONCLUIDA (PR #158, aguardando so o merge)
> Atualizado no fim do dia 2026-07-09. O acabamento foi feito e, no meio dele, a
> revisao achou um bug de raiz: a feature **so conseguia restringir menu, nunca
> liberar** (o sidebar filtrava antes por `visibleTo`/`superAdminOnly` e os layouts
> chamavam `requireMinRole` fixo). Corrigido: `menu_access` virou a autoridade unica
> dos menus de topo. Junto vieram: seletor duplicado do menu "Relatorios 2.0"
> eliminado, guarda no Dashboard (que era configuravel mas abria por URL, e era o
> destino de todo redirect de bloqueio), Configuracao fixa em super_admin,
> `temp-rules.ts` removido e drift da migration zerado.
>
> Verificacao: tsc/eslint/next build verdes; jest 3767; **E2E visual contra o dev real
> com 16 asserções verdes**, e o mesmo E2E rodado contra o codigo anterior falha
> exatamente nas 3 asserções de "liberar". As 105 migrations aplicam do zero em banco
> limpo e `migrate diff` diz "No difference detected".
>
> **Falta so o merge do PR #158** (dispara deploy; exige "sim" do usuario,
> via `python3 scripts/ship.py`). Detalhes em `.agente-handoff.md` da worktree.

<details>
<summary>Estado anterior (quando este handoff foi escrito, de manha)</summary>

**Configuracao de acesso aos menus por perfil.** Branch `feat/config-acesso-menus`.
Nucleo CONSTRUIDO e validado (tsc + TDD 4 casos + build de prod, todos verdes):
- Modelo `MenuAccess` + migration idempotente `20260709010000_menu_access`.
- `src/lib/nav/menu-catalog.ts` (catalogo dos 8 menus + logica pura podeVerMenu),
  `menu-access.ts`, `require-menu-access.ts` (guard), `menu-access.test.ts`.
- `src/lib/actions/menu-acesso.ts` (server action).
- `src/components/configuracao/menu-access-card.tsx` (UI SegmentedControl por perfil,
  Comum/Administracao, selo "Voce sempre ve") montado em configuracao/page.tsx.
- Integracao sidebar (layout.tsx + sidebar.tsx filtra por podeVerMenu).
- Guard aplicado em usuarios/relatorios (referencia do padrao).

Decisoes do usuario: (1) todos os 8 menus configuraveis; (2) super_admin SEMPRE ve
Configuracao (trava anti-lockout).

**FALTA (retomar):** (a) E2E VISUAL contra dado real (subir dev da worktree, logar
super_admin, salvar nivel, ver menu sumir/aparecer no sidebar + guard por URL);
(b) revisao ui-ux-pro-max do card; (c) replicar requireMenuAccess nas pages restantes;
(d) validar migration em banco limpo (drift zero); (e) abrir/mergear , PR #158 ja
existe, MERGE so com "sim" do usuario (dispara deploy). Ler docs/runbooks/deploy-procedure.md.

**Como retomar:** `agente start feat/config-acesso-menus` (recria worktree, npm ci),
ler `.agente-handoff.md` + `docs/BRIEFING-config-acesso-menus.md` na branch. A tabela
menu_access ja existe no banco DEV.

</details>

## localhost
Durante a sessao anterior o localhost:3000 ficou servindo a branch feat/config-acesso-menus
(para o usuario ver o card). Decidir se volta pra main (`agente up main`).
