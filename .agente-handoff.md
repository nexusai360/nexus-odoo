# HANDOFF , feat/config-acesso-menus (Frente B) , RETOMADA 2026-07-09

> LEIA PRIMEIRO. Feature "Configuracao de acesso aos menus por perfil". O NUCLEO ja
> foi construido, testado (tsc/TDD/build verdes) e esta no **PR #158** (nao mergeado).
> Proposta tecnica completa + mapeamento do RBAC em `docs/BRIEFING-config-acesso-menus.md`.

## COMO RETOMAR
A worktree pode ter sido deletada. A branch `feat/config-acesso-menus` esta no GitHub
(PR #158). Recrie a worktree: `agente start feat/config-acesso-menus` (ou checkout da
branch) e leia este arquivo + o BRIEFING. `npm ci` + `npx prisma generate`. A tabela
`menu_access` ja existe no banco DEV compartilhado (foi aplicada via SQL).

## JA CONSTRUIDO (PR #158, 7 commits, tsc/TDD/build verdes)
- **Modelo `MenuAccess`** (prisma) + **migration formal idempotente**
  `20260709010000_menu_access` (IF NOT EXISTS).
- **`src/lib/nav/menu-catalog.ts`**: os 8 menus (key/label/href/secao/padrao/travadoSuperAdmin)
  + logica PURA `podeVerMenu`/`defaultMenuAccess`. Trava anti-lockout: `configuracao`
  travadoSuperAdmin=true (super_admin sempre ve).
- **`src/lib/nav/menu-access.ts`**: obterMenuAccess/definirMenuAccess (upsert).
- **`src/lib/nav/menu-access.test.ts`**: 4 casos TDD (verde).
- **`src/lib/nav/require-menu-access.ts`**: guard de rota server-side.
- **`src/lib/actions/menu-acesso.ts`**: server action salvarMenuAccess (super_admin only).
- **`src/components/configuracao/menu-access-card.tsx`**: UI (SegmentedControl por perfil,
  agrupado Comum/Administracao, otimista+toast, selo "Voce sempre ve"). Montado em
  `configuracao/page.tsx` (coexiste com o card fino de Relatorios 2.0).
- **Integracao sidebar**: `layout.tsx` resolve obterMenuAccess() e passa ao Sidebar;
  `sidebar.tsx` filtra os itens de topo por podeVerMenu.
- **Guard aplicado** em `usuarios/page.tsx` e `relatorios/page.tsx` (REFERENCIA do padrao).

## DECISOES DO USUARIO (nao rediscutir)
1. TODOS os 8 menus configuraveis. 2. Trava: super_admin SEMPRE ve Configuracao.

## FALTA (o acabamento , continuar aqui)
1. **Replicar `requireMenuAccess("<key>")`** no topo das pages restantes (apos o redirect
   de auth): dashboard NAO (e o destino do redirect , deixar livre), diretoria/relatorios-2/
   agente/configuracao/integracoes ja tem guards proprios de role, mas idealmente alinhar.
   Minimo pendente: nenhum critico (usuarios+relatorios cobrem os casos-exemplo).
2. **E2E VISUAL**: subir o dev DA worktree (`npm run dev` na worktree, porta 3000),
   logar super_admin, abrir /configuracao, salvar um nivel num menu e confirmar que o
   item some/aparece no sidebar + guard bloqueia URL direta. (dado real, obrigatorio.)
3. **Revisao ui-ux-pro-max** do card (espacamento/responsivo).
4. **Migration em PROD**: no deploy, a `20260709010000_menu_access` aplica via
   `prisma migrate deploy` do entrypoint (idempotente). Validar em banco limpo (drift zero)
   como foi feito na frente anterior. Ler `docs/runbooks/deploy-procedure.md`.
5. **PR #158 -> merge**: SO com "sim" explicito do usuario (dispara deploy).

## ESTADO DA PLATAFORMA
- Frente anterior (Diretoria+Nex+Config+Relatorios2.0/F6) EM PRODUCAO (agentenex.nexusai360.com),
  deploy validado, 103 migrations. main = commit 448dc67e + este PR pendente.
- Banco Postgres compartilhado , migration nova segue protocolo de schema.
- localhost:3000: durante esta sessao ficou servindo ESTA branch (feat/config-acesso-menus)
  para o usuario ver o card. Ao retomar/limpar, decidir se volta pra main (`agente up main`).
