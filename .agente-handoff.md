# Handoff da worktree , feat/menu-diretoria (2026-07-08)

**Sub-projeto:** Inteligência de Demanda (branch `feat/menu-diretoria`). Tudo LOCAL,
NADA mergeado. NUNCA mergear `main` sem "sim" explícito do usuário.

## LER PRIMEIRO
`docs/superpowers/plans/2026-07-08-HANDOFF-inteligencia-demanda.md` , estado completo,
próxima ação, armadilhas, comandos, metodologia. Depois `...EXECUCAO-PROGRESSO.md` e o
dossiê `docs/superpowers/specs/pericia-fluxos-2026-07/`.

## Estado (resumo)
Ondas 0/A/B/C + tabela do Nex COMPLETAS e testadas no dado real. Catálogo 128 tools,
integração 53/53. Demanda ABERTA=395 pedidos/R$77,6M. 6 tools novas + `fato_pedido_item` +
motor de classificação materializado (`bucket_demanda`/`is_venda_externa`) + tabela GFM no
chat do Nex (parser TDD + 2 renderers + regra de prompt 8-tab).

## Próxima ação (única pendência)
**Painéis da diretoria**: integrar `bucket_demanda` ao builder de layout existente
(`src/lib/diretoria/builder/`), trocar a lógica antiga `queryDemandasPendentes` pela nova
com paridade de dado, RBAC/menu. Detalhes no §2 do HANDOFF. UI só na sessão principal +
ui-ux-pro-max. Validar também a tabela na tela (subir dev, perguntar demanda ao Nex).

## Não repetir (armadilhas , HANDOFF §3)
Prisma value quebra jest do mcp (usar $queryRaw parametrizado); cast int em jsonb via CASE
regex; migration drift F6 = ALTER manual + prisma generate; rebuild worker via `app`;
produto_id != código no nome; goldens +1 por tool + gen:mcp-catalog. Reviews spec/plan
SEQUENCIAIS. Modelo Opus. Cron de 10 min ligado.
