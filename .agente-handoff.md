# HANDOFF , feat/conexao-whatsapp , 2026-07-10

## Estado: EXECUÇÃO 100% COMPLETA , aguardando MERGE (só com "sim" explícito)

As 9 ondas do PLAN v3 foram entregues em TDD, com commits atômicos, tudo verde:
tsc=0, eslint=0, jest 3878/0, next build OK, db-health saudável, drift zero.
E2E contra o dev real: 19/19 (scripts/e2e-isolamento-conexao.ts).

**PR aberto: https://github.com/nexusai360/nexus-odoo/pull/162**

## O que falta

1. Usuário validar (opcional: /ultrareview) e dizer "sim" para o merge.
2. Após o merge: Shepherd deploya sozinho (~5 min). Validar /api/health.
3. Migrations desta branch estão aplicadas SÓ NO DEV; `migrate deploy` roda no
   boot do app em prod (padrão do projeto). Backfill em prod atinge só o
   Matrix Group (fica response_mode NULL = direct efetivo até configurarem o
   Envio pela tela de edição, que grava n8n_webhook).

## Decisões do usuário respeitadas

- Trava por NÚMERO nos dois sentidos (SPEC §3.4.1) , implementada.
- Prompt do agente NÃO mudou (conversão tabela→lista é código puro).
- Nenhum texto de produto cita n8n (teste automatizado varre a UI).
- Botão "Concluir configuração e continuar" nas etapas 1 e 2 , feito.
- Merge só com "sim" , PENDENTE.

## Armadilhas descobertas nesta sessão (além das anteriores)

- **Nunca rode `docker compose up` da WORKTREE:** ela não tem `.env`, e o
  worker recriado fica sem ODOO_* e entra em crash-loop. Recrie da pasta
  principal com `--no-build` (a imagem já foi buildada da worktree).
- No E2E, o bloqueio dispara do APP (host → localhost) e a resposta final do
  WORKER (container → host.docker.internal). Os targets congelam no enqueue.
- Porta 3100 (mcp) estava ocupada por um node local; o container mcp não subiu
  (sem impacto nesta feature).
