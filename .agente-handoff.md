# Handoff , feat/diretoria-entregas-estoque

**Quando:** 2026-07-19 (troca de sessao pedida pelo dono; handoff script NAO rodado, a pedido)
**Worktree:** `branches/feat-diretoria-entregas-estoque/`
**Branch:** `feat/diretoria-entregas-estoque`
**PR:** https://github.com/nexusai360/nexus-odoo/pull/196 (ABERTO, sem merge , aguarda o dono)

## Estado

- HEAD: `23f339f0` (confira com `git log --oneline -1`)
- tsc 0, jest verde (~4317 testes). Sem PR/merge (regra: so o dono libera).
- Containers dev (app/mcp/worker) PARADOS; so db e redis de pe. Ao subir dev/containers, rebuildar
  (`docker compose build app` + `up -d --build mcp`) ANTES , senao worker velho zera colunas novas.

## O que foi ENTREGUE nesta sessao (nao refazer)

- **PLAN 1** (ajustes finos) COMPLETO: modalidade de frete materializada no fato_pedido (4 pontas),
  regra JDSDEMO, demonstracao em 2 blocos no painel. Plano: `docs/superpowers/plans/2026-07-19-plan1-ajustes-finos.md`.
- **PLAN 2** (No do Mercos) COMPLETO: coluna numero_mercos parseada do obs (4 pontas) + busca reversa 1:N.
  Plano: `docs/superpowers/plans/2026-07-19-plan2-numero-mercos.md`.
- **PLAN 3** (composicao de valor dos kits) EM EXECUCAO. Ver abaixo.

## PLAN 3 , ponto exato de retomada

Plano FINAL v3: `docs/superpowers/plans/2026-07-19-plan3-composicao-valor-kits.md`
Pericia (o dono exigiu): `docs/superpowers/research/2026-07-19-plan3-pericia-completa-valor-kits.md`
Decisao do dono: `docs/superpowers/research/2026-07-19-plan3-decisao-dono-kits-valor.md`

FEITOS e verdes (committados):
- V1 `src/lib/estoque/desmembrar-valor.ts` (rateio proporcional, fechamento por maior resto).
- W1 colunas `lista_data_ativacao`/`lista_inativa` no `fato_lista_material_item` + builder
  `src/worker/fatos/fato-lista-material.ts` le o header `raw_sped_produto_lista_material`.
- W2 `src/lib/estoque/resolver-bom.ts` (escolhe lista ativa SO em multi-lista; lista unica passa
  reto; all-inactive usa todas; nunca zera).

RETOMAR DE **W3** e seguir ate o fim (X1 -> Y -> Z -> pericia da onda), TDD, sem PR/merge:
- **W3**: em `src/lib/diretoria/queries/estoque.ts` (~linha 1030-1050) a montagem do `bomPorPai` da
  necessidade de compra hoje empilha TODAS as listas (duplica componente em kits multi-BOM). Trocar
  por `resolverBom` por pai (trazendo `listaDataAtivacao`/`listaInativa` do fato). E2E: kit 1281
  (POWERMILL) nao duplica; os 131 kits de lista unica ficam com a MESMA contagem de componentes.
- **X1a-d**: `src/lib/reports/queries/composicao-kit.ts` (`queryComposicaoKit`). Base do valor =
  preco de venda de TABELA (Venda Padrao, tabela_id=3 em fato_preco); venda real (fato_pedido_item.
  vr_produtos) so como visao secundaria com n>=5 (MEDIANA + expor nVendas). Pesos = quantidade x
  preco_custo do componente; `desmembrarValor(Math.round(reais*100), pesos)` e `/100` na saida.
  Componente sem custo NEM venda -> `coberturaCompleta=false`, NAO ratear como % (mostra custo/tabela
  direto). Interface no plano. Sanitizar em dash do ERP.
- **Y1-Y5**: painel de composicao de valor dos kits na Diretoria (rota Estoque). ui-ux-pro-max
  OBRIGATORIO antes de tocar UI, inline (nunca subagente). Barra estrutura vs painel, tabela
  componentes (custo/venda/rateado/%), badge Matrix/acessorio, aviso cobertura incompleta,
  dark+light, 375px, sem emoji, sem travessao.
- **Z1a-c**: tool `mcp/tools/estoque/composicao-kit.ts` (+ index/catalog/triggers) + BI schema
  (`src/lib/agent/bi-schema-reference.ts`) + vocabulario (`src/lib/agent/router/domain-vocabulary.ts`).
- **Pericia da onda** (subagente adversarial) apos, e reportar ao dono "painel vale 14-25%, nao zero".

Depois do PLAN 3: **PLAN 4** (infra estoque: ingerir `usage` do stock.location -> DSTOCK/transito,
ver doc-mae §5/§11) e **PLAN 5** (job de atendimento popular quantidade_a_atender).

## Diretrizes inegociaveis (doc-mae §10)
1. ERP Odoo = fonte da verdade. 2. Consistencia nas 4 pontas (Diretoria, Relatorios 1.0, 2.0, Nex).
3. Metodologia por PLAN: v1 -> review adversarial -> v2 -> review mais profunda -> v3 -> TDD ->
   pericia da onda. Sempre Opus nos subagentes. 4. Sem PR/merge ate o dono liberar. 5. Sem travessao.

## Ambiente
Banco dev: `docker exec nexus-odoo-db-1 psql -U nexus -d nexus_odoo_l1 -tAc "SQL"` (porta 5436).
Ler primeiro: STATUS.md (topo), este arquivo, o plano do PLAN 3, e a pericia do PLAN 3.
